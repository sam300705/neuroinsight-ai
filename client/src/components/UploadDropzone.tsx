import { FileWarning, FileUp, Image, ScanLine } from "lucide-react";
import { useCallback, useRef, useState } from "react";
import type { AnalysisMode } from "@shared/neuroinsight";

export type LocalFileCheck = { valid: boolean; messages: string[]; previewUrl?: string };
export function validateLocalFile(file: File, mode: AnalysisMode): LocalFileCheck { const extension = file.name.toLowerCase().split(".").slice(1).join("."); const maxBytes = 50 * 1024 * 1024; const expected = mode === "classification" ? ["png", "jpg", "jpeg"] : ["nii", "nii.gz"]; const acceptableMime = mode === "classification" ? ["image/png", "image/jpeg"] : ["application/x-nifti", "application/nifti", "application/gzip", ""]; const messages: string[] = []; if (!expected.includes(extension)) messages.push(`Selected mode supports ${expected.map(item => `.${item}`).join(" or ")} files only.`); if (!acceptableMime.includes(file.type)) messages.push("The browser-reported MIME type is incompatible with the selected mode."); if (file.size === 0) messages.push("The selected file is empty."); if (file.size > maxBytes) messages.push("The selected file exceeds the 50 MB upload limit."); return { valid: messages.length === 0, messages, previewUrl: mode === "classification" && messages.length === 0 ? URL.createObjectURL(file) : undefined }; }
export function UploadDropzone({ mode, onSelect }: { mode: AnalysisMode; onSelect: (file: File, check: LocalFileCheck) => void }) { const [dragging, setDragging] = useState(false); const inputRef = useRef<HTMLInputElement>(null); const accept = mode === "classification" ? ".png,.jpg,.jpeg,image/png,image/jpeg" : ".nii,.nii.gz,application/x-nifti,application/nifti,application/gzip"; const select = useCallback((file?: File) => { if (file) onSelect(file, validateLocalFile(file, mode)); }, [mode, onSelect]); return <div onDragOver={event => { event.preventDefault(); setDragging(true); }} onDragLeave={() => setDragging(false)} onDrop={event => { event.preventDefault(); setDragging(false); select(event.dataTransfer.files[0]); }} className={`rounded-2xl border-2 border-dashed p-8 text-center transition ${dragging ? "border-teal-600 bg-teal-50" : "border-slate-300 bg-slate-50"}`}><input ref={inputRef} type="file" accept={accept} className="sr-only" onChange={event => select(event.target.files?.[0])} /><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-white text-teal-800 shadow-sm">{mode === "classification" ? <Image className="size-6" /> : <ScanLine className="size-6" />}</span><h2 className="mt-4 font-semibold">Drop a supported MRI file here</h2><p className="mx-auto mt-2 max-w-lg text-sm leading-6 text-slate-600">{mode === "classification" ? "Mode A accepts PNG or JPEG. The client checks extension, browser MIME type, emptiness, and 50 MB size before server-side verification." : "Mode B accepts a compatible NIfTI .nii or .nii.gz volume. Server-side processing will verify structure, metadata, and modality compatibility."}</p><button type="button" onClick={() => inputRef.current?.click()} className="mt-5 inline-flex items-center gap-2 rounded-xl bg-teal-800 px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-teal-900 focus:outline-none focus-visible:ring-2 focus-visible:ring-teal-700"><FileUp className="size-4" />Choose file</button><p className="mt-4 flex justify-center gap-2 text-xs text-slate-500"><FileWarning className="size-4" />Browser validation is preliminary; content and corruption checks happen on the analysis service.</p></div>; }


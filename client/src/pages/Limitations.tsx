import { AlertTriangle, CheckCircle2 } from "lucide-react";
import { AcademicDisclaimer } from "@/components/ResearchDisclaimers";
import { copyFor } from "@/contexts/pageCopy";
import { useLanguage } from "@/contexts/LanguageContext";
const limits = ["The product is an academic proof-of-concept and not a certified diagnostic system, medical device, or replacement for a qualified clinician.", "The four-class 2D classifier and the glioma-focused volumetric segmentation service use separate dataset and model scopes; neither implies universal tumor segmentation.", "Model confidence scores are not medical probabilities unless calibration has been completed and evaluated for the deployed model.", "Grad-CAM is coarse classifier attribution, not a tumor boundary, causal explanation, or proof of correctness.", "Physical size values require confirmed pixel or voxel spacing; otherwise the product reports only relative counts and occupancy.", "Public datasets may lack patient-level metadata, acquisition context, and representative clinical coverage. Generalisation must not be assumed.", "Raw scans are not persisted by default, and the contextual assistant must not offer diagnosis or treatment advice."];
export default function Limitations() { const { language } = useLanguage(); const text = copyFor(language).limitations; return <div className="mx-auto max-w-5xl space-y-6"><div><h1 className="text-3xl font-semibold tracking-tight">{text.title}</h1><p className="mt-3 max-w-3xl leading-7 text-slate-600">{text.intro}</p></div><AcademicDisclaimer /><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><ul className="space-y-4">{limits.map(item => <li key={item} className="flex gap-3 text-sm leading-6 text-slate-700"><AlertTriangle className="mt-0.5 size-5 shrink-0 text-amber-700" />{item}</li>)}</ul></section><section className="rounded-2xl bg-emerald-50 p-6 text-emerald-950"><div className="flex gap-3"><CheckCircle2 className="size-5 shrink-0 text-emerald-700" /><p className="text-sm leading-6">When a result is low-confidence, incompatible, unavailable, or outside the stated scope, the correct interface response is to recommend qualified expert review—not to infer safety or normality.</p></div></section></div>; }

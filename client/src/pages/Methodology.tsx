import { Database, Layers3, ShieldCheck } from "lucide-react";
import { AcademicDisclaimer, GliomaScopeDisclaimer, GradCamDisclaimer } from "@/components/ResearchDisclaimers";
import { copyFor } from "@/contexts/pageCopy";
import { useLanguage } from "@/contexts/LanguageContext";
const stepNumbers = ["01", "02", "03", "04"];
const cardIcons = [Database, Layers3, ShieldCheck];
export default function Methodology() { const { language } = useLanguage(); const text = copyFor(language).methodology; return <div className="mx-auto max-w-6xl space-y-6"><div><h1 className="text-3xl font-semibold tracking-tight">{text.title}</h1><p className="mt-3 max-w-3xl leading-7 text-slate-600">{text.intro}</p></div><AcademicDisclaimer /><div className="grid gap-4 md:grid-cols-2">{text.steps.map(([title, detail], index) => <article key={title} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-semibold tracking-[0.15em] text-teal-700">{stepNumbers[index]}</p><h2 className="mt-3 text-lg font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{detail}</p></article>)}</div><section className="grid gap-4 rounded-2xl bg-teal-900 p-6 text-white md:grid-cols-3">{text.cards.map(([title, detail], index) => { const Icon = cardIcons[index]!; return <div key={title}><Icon className="size-5 text-teal-200" /><h2 className="mt-3 font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-teal-50">{detail}</p></div>; })}</section><GradCamDisclaimer /><GliomaScopeDisclaimer /></div>; }

import { Database, Layers3, ShieldCheck } from "lucide-react";
import { AcademicDisclaimer, GliomaScopeDisclaimer, GradCamDisclaimer } from "@/components/ResearchDisclaimers";
const steps = [["01", "Audit data before use", "Verify provenance, licence, patient-level split feasibility, duplicates, masks, and intended mode before any download or training."], ["02", "Train by mode", "Compare transfer-learning classifiers for four-class 2D images separately from a whole-tumor segmentation model for compatible glioma-focused volumes."], ["03", "Validate uncertainty", "Choose architecture, calibration, and abstention thresholds on validation data; reserve the test set for the final evaluation."], ["04", "Explain honestly", "Expose model confidence as a score, render Grad-CAM as attribution, and separate it from segmentation masks and physical measurement."]];
export default function Methodology() { return <div className="mx-auto max-w-6xl space-y-6"><div><h1 className="text-3xl font-semibold tracking-tight">Methodology and safeguards</h1><p className="mt-3 max-w-3xl leading-7 text-slate-600">The application uses a two-mode research design to avoid conflating incompatible datasets, imaging dimensions, clinical scope, and model outputs.</p></div><AcademicDisclaimer /><div className="grid gap-4 md:grid-cols-2">{steps.map(([number, title, detail]) => <article key={number} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-semibold tracking-[0.15em] text-teal-700">{number}</p><h2 className="mt-3 text-lg font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{detail}</p></article>)}</div><section className="grid gap-4 rounded-2xl bg-teal-900 p-6 text-white md:grid-cols-3"><div><Database className="size-5 text-teal-200" /><h2 className="mt-3 font-semibold">Manifest-driven data</h2><p className="mt-2 text-sm leading-6 text-teal-50">The planned pipeline logs data version, checksums, source, modality, class, and mask availability without committing raw data.</p></div><div><Layers3 className="size-5 text-teal-200" /><h2 className="mt-3 font-semibold">Separate outputs</h2><p className="mt-2 text-sm leading-6 text-teal-50">Classification, attribution, segmentation, and physical measurement have distinct evidence requirements and UI labels.</p></div><div><ShieldCheck className="size-5 text-teal-200" /><h2 className="mt-3 font-semibold">Manual review state</h2><p className="mt-2 text-sm leading-6 text-teal-50">Low confidence, incompatible inputs, and unavailable models never produce reassuring or definitive medical language.</p></div></section><GradCamDisclaimer /><GliomaScopeDisclaimer /></div>; }


# Visual Verification Log

## 2026-08-21 — Dashboard usability check

The desktop views for the overview, analysis, results, and history routes were captured at 1280×720. The persistent academic-use disclaimer, primary navigation, validation controls, empty-result state, and history filters rendered without visible overlap or clipped content.

The same routes were captured at 375×812. The mobile header retained a visible language switcher and menu trigger; the research-only banner remained visible; cards, upload controls, and unavailable-model states remained within the viewport without horizontal overflow. The screenshots did not exercise a Hindi toggle interaction, so Hindi language-switch interaction remains covered by implementation review and planned automated coverage rather than this visual session.

## 2026-08-21 — FastAPI validation-service smoke test

An isolated FastAPI process was started on `127.0.0.1:8011` with `CORS_ALLOWED_ORIGINS=http://localhost:3000`. The `/health` endpoint returned `status: ok`; `/ready` returned `ready: false` with the explicit no-verified-model reason; and a multipart request labelled as a PNG but containing corrupt bytes returned HTTP `422` with the authoritative image-decode validation message. The temporary process was terminated after the check. This test verifies input validation and honest unavailable-model behavior only; it does not verify model inference.

## 2026-08-21 — Keyboard-navigation shell check

After adding the global skip link, localized navigation-control names, and the focusable main-content target, mobile captures of the overview and analysis routes at 375×812 retained the visible research-use banner, language switcher, menu trigger, headings, scoped mode cards, and upload guidance without layout overlap.

## 2026-08-22 — Real Mode A local inference and report check

The FastAPI service was started temporarily with the local EXP-005 BDNeuro-MRI ResNet50 checkpoint and its validation-only calibration JSON explicitly configured. `/health` returned `status: ok`; `/ready` returned `ready: true` and the non-clinical experimental-scope reason. A public fixed-split BDNeuro-MRI test image was submitted to `/api/v1/classify`; the real response contained `status: complete`, a predicted class, a validation-calibrated model-confidence score, and a 82,885-byte response containing a real Grad-CAM PNG payload. The same unmodified analysis response and its Grad-CAM payload were used to create a 65 KB, two-page PDF through `/api/v1/report`. The temporary process was terminated afterward to avoid retaining a model process in the sandbox. This check establishes local software-path behavior only, not patient-level, external, clinical, or diagnostic validation.

## 2026-08-22 — Browser endpoint configuration check

The development dashboard was configured to the temporary HTTPS local FastAPI proxy. A Vitest health check against the configured public base URL passed. The analysis page loaded with its visible research warning, Mode A scope card, accessible file-picker control, and server-side validation messaging. The automated browser uploader could not address the hidden native file input in this environment even after the visible picker was activated; this is an automation limitation, not a successful browser upload claim. The same request path remains covered by the passing server-response and corrupt-upload client tests plus the local service smoke test.

## 2026-08-22 — Verified external ONNX Runtime service

The Vercel ONNX Runtime deployment passed HTTPS `/health`, `/ready`, and model-information checks, along with CORS preflight from the exact managed dashboard preview origin. A malformed image upload received HTTP `422`. One lawful public BDNeuro-MRI fixed-split test image then produced a real complete Mode A response with a validation-calibrated model confidence score and real Grad-CAM. The deployment classified that glioma-labelled image as **meningioma** at `0.825931191444397`; this error was retained in the verification record and was not portrayed as diagnostic performance or hidden. The exact response plus real Grad-CAM generated a valid two-page PDF report after an external FPDF layout correction. The external service stores no uploaded MRI image as a dashboard artifact.

## 2026-08-22 — Browser-level upload and result checks

The native file input was changed from a screen-reader-only control to a transparent, browser-targetable control over the upload area. A saved Playwright script injected a corrupted PNG payload, confirmed the signature-error text, and confirmed that **Validate and continue** remained disabled before any inference request. A separate saved Playwright script submitted the same lawful public fixed-split image through the managed dashboard, waited for the results route, and verified the rendered **Experimental academic result**, exact non-diagnostic notice, non-medical confidence wording, and protected-save consent control. Neither browser test asserted class correctness, persisted the original upload, or tested authenticated history retrieval. The latter requires an account session and remains an explicit open verification gate.

The saved accessibility browser check also passed. It used the keyboard to focus and activate the skip-to-main-content link, verified focus reached the `main-content` target, verified the native MRI input has a clear accessible name, and verified corrupted-file feedback is conveyed by visible text alongside the disabled submission state rather than colour alone.

An axe-core/Playwright audit then ran WCAG 2 A/AA rules, including colour contrast, across the eight primary routes. It initially found the small sidebar navigation label at insufficient contrast and a viewport `maximum-scale=1` restriction. The label was changed from `slate-400` to `slate-500`, the zoom restriction was removed, and the full cross-route audit passed with keyboard focus reaching the skip link on every route.

The protected artifact lookup was further tightened in code to verify the requesting account owns the scan record and then issue a fresh signed object-storage download URL. This preserves the no-raw-upload policy and avoids returning a durable storage path directly. At this point in the historical sequence, the authenticated live retrieval interaction had not yet been exercised; the subsequent 2026-08-22 entry records the owner-approved signed-in verification.

The Hindi real-inference browser test passed after the real-result and protected-save UI states were localized. It switched the interface language, submitted the lawful public fixed-split image, and verified Hindi experimental-result and save-consent labels. The exact visible notice **“This system is not a medical diagnosis and must not replace a qualified radiologist.”** remained present as required.

## 2026-08-22 — Authenticated derived-artifact history verification

With the user signed in through the secure dashboard flow and explicit confirmation for the temporary test record, one lawful public fixed-split BDNeuro image completed the live Mode A path. The system saved account-linked pseudonymous result metadata, the server-generated PDF, and the real Grad-CAM overlay while explicitly confirming that the original upload was not stored. The History page listed both derived artifacts. The report action resolved only after the authenticated ownership check and opened a fresh signed URL in the browser PDF viewer. The temporary test record was then deleted, leaving History empty. This proves the protected Mode A artifact workflow; it does not create or validate Mode B segmentation artifacts.

## 2026-08-22 — Published dashboard CORS and live inference check

The dashboard was published at `https://neuroaiapp-gtbxy6cw.manus.space`. The Vercel backend was redeployed with that exact origin plus localhost development origins only. A preflight from the published origin succeeded and an unrelated origin was rejected without an allow-origin header. After the dashboard environment configuration was rebuilt, the saved real-inference browser test passed against the public dashboard. It rendered the real experimental Mode A result and protected-save consent control from the deployed external service. This is a live connectivity verification, not a claim of model correctness, diagnostic use, or authenticated history retrieval.

## 2026-08-27 — PR preview audit and fail-closed remediation

Vercel reported a `READY` Git preview for PR #1 commit `3252cb9` at the time of review. Its `/api/v1/model-info` request nevertheless returned `500 FUNCTION_INVOCATION_FAILED`, while build logs showed no build error. This was treated as a preview-only resilience defect, not as evidence about the already owner-approved managed dashboard release or a production model failure. The branch now catches external-artifact, checksum, metadata, and ONNX-initialization exceptions during startup, logs only the exception type, and exposes an honest unavailable classifier state instead of crashing the function. Final preview verification is required after the branch is pushed; no managed dashboard publication is authorized by this remediation.

## 2026-08-27 — Current public endpoint boundary re-check

The externally configured production inference endpoint returned `status: ok` from `/health` and `ready: true` with the academic non-clinical scope from `/ready`. Its model-information response reported EXP-005 classification as available and segmentation as unavailable. An `OPTIONS` preflight from `https://neuroaiapp-gtbxy6cw.manus.space` returned `200` with that exact `access-control-allow-origin`; an unrelated HTTPS origin returned `400` without an allow-origin header. This re-check covers only the previously published public endpoint, not the unpushed branch changes or the current Vercel PR preview.

## 2026-08-27 — Current-head GitHub Actions verification

GitHub Actions pull-request workflow [`33038209553`](https://github.com/sam300705/neuroinsight-ai/actions/runs/33038209553) completed successfully for branch commit `8d5d02f`. It ran frozen Node installation, TypeScript, Vitest, production build, the 768,000-byte bundle guard, production dependency audit, raw-artifact hygiene scan, the newly added local production-browser corrupt-upload/accessibility/route-accessibility checks, FastAPI tests, and ML/data tests. The GitHub token could not read workflow annotations (`403`), but the job itself completed successfully. This workflow result does not publish the managed dashboard.

The Vercel Git preview for that same commit, deployment `dpl_JDAhZbVBK4xU9gPpcxmmM9YdfZhH`, reached `READY` and returned a structured `200` model-information response instead of the earlier function-invocation failure. It reported both classification and segmentation as `unconfigured`/`unavailable`, which is the intended fail-closed preview state when the branch deployment lacks a usable verified external EXP-005 artifact. This does not change the existing production endpoint, which separately reports EXP-005 classification available, and does not authorize a managed-dashboard publication.

The current public managed dashboard still shows the owner-approved recovery release rather than the branch. It visibly preserves the exact non-diagnostic warning, live Mode A and unavailable Mode B boundaries, but retains the older phrase “Anonymized scan history” on its overview page. The current PR branch corrects that text to account-linked pseudonymous metadata, but publishing that correction requires a separate owner approval. The observed wording is therefore a release-version difference, not evidence that the branch implementation has regressed.

## 2026-08-27 — Final artifact-initialization boundary

The final branch preview deployment `dpl_BkMcnXKW4iVfuEvWh6KXh172fesR` is `READY` and its capability endpoint returns structured `200` data rather than a `500`. It reports both modes unavailable because the startup guard logged `classifier_initialization_failed:error_type=ValueError`. Read-only HTTPS header checks confirmed that the configured ONNX, metadata, and calibration URLs respond successfully; in-memory checks confirmed the committed metadata and calibration SHA-256 values. The 94 MB ONNX checkpoint was not downloaded or replaced because this final review is limited to metadata/manifest verification and must not alter a model artifact. The failure is therefore recorded as an owner-controlled release-readiness gate before any new Vercel production deployment, while the existing production endpoint remains independently available.

## 2026-08-27 — Final PR branch verification

GitHub Actions pull-request run [`33045574801`](https://github.com/sam300705/neuroinsight-ai/actions/runs/33045574801) passed at branch commit `d2441404cdfe6473e05d4c64d3361abcfc068066`. It ran frozen installation, TypeScript, **45** Vitest regressions, production build, the 768,000-byte initial-bundle guard, production dependency audit, raw-artifact hygiene, deterministic local production-browser checks, **25** FastAPI regressions, and **8** ML/data regressions.

The automatic Vercel preview for the same exact branch commit, `dpl_8XSnrDvFQSmTDivXjrMH8MqdAhKh`, reached `READY`. Its deployment-specific `/api/v1/model-info` endpoint returned structured `200` capability data: Mode A remains safely `unconfigured`/`unavailable` under the artifact-initialization gate, and Mode B remains `unconfigured`/`unavailable`. No test uploaded an image, no production target changed, and no managed-dashboard publication occurred.

## 2026-08-27 — EXP-005 preview initialization root cause and recovery

The branch-preview failure was traced to a **contract mismatch**, not a bypassable download or checksum error: the audited EXP-005 ledger and verified metadata specify ResNet50 preprocessing at **160 pixels**, while the newly introduced ONNX contract incorrectly required 224 pixels. The strict contract was corrected to the audited 160-pixel value. SHA-256 verification, HTTPS-only downloads, metadata/calibration validation, and fail-closed behavior remain enabled.

The ONNX bootstrap now emits a non-sensitive internal category (`artifact_missing`, `download_failed`, `checksum_mismatch`, `contract_mismatch`, `metadata_invalid`, or `onnx_initialization_failed`) while the public API continues to reveal only an unavailable capability state if startup fails. The branch preview deployment for commit `c2bb771` became `READY`; `/health` returned `ok`, `/ready` returned `ready: true`, and `/api/v1/model-info` reported EXP-005 classification `available` while Mode B remained `unavailable`. A single checksum-matched, sanitized **non-test public BDNeuro training image** was submitted once for deployment verification only. It completed a real Mode A inference with the EXP-005 model version, calibrated output, required manual-review flag, and no claim of clinical or held-out performance. No raw image, model checkpoint, artifact URL, or response-derived Grad-CAM was retained in the repository.

## 2026-08-27 — Optional assistant deterministic verification

The new Research Explanation Assistant was checked without a credential or live provider request. The browser request serializer is restricted to bounded de-identified derived fields and was tested to exclude filenames, scan IDs, previews, Grad-CAM binary data, raw analysis objects, account data, URLs, storage keys, tokens, and secrets. The FastAPI schema forbids unrecognised request fields. Provider-mocked tests exercise strict OpenAI and Gemini JSON-schema request construction, selected-provider failure/timeout fallback, malformed or unsafe response fallback, and deterministic medical/prompt-injection refusals that execute before a provider call.

A controlled FastAPI endpoint regression selected `AI_PROVIDER=openai` while omitting its key/model. It returned the existing offline FAQ with a safe request ID and emitted only `provider=offline_faq`, outcome, category, latency, and request ID; the test asserts the question was absent from logs. The assistant has an independent 10-per-minute process-local limit. This is a local process boundary, not a distributed rate-limit claim. No provider was enabled, no secret was added, no production/service configuration was changed, and no public dashboard or Vercel production deployment was published.

## 2026-08-27 — Exact-head assistant preview verification

GitHub Actions run [`33048397462`](https://github.com/sam300705/neuroinsight-ai/actions/runs/33048397462) completed successfully for commit `a1c8fb571c4f0feda26c70705c3ccab316397563`. The matching Vercel Git preview deployment `dpl_JBCWYbDe7AFKUoSqNqLDy81pXUwQ` is `READY` at `https://neuroinsight-ai-inference-hm7vl9x0q-sam300705s-projects.vercel.app`. Its `/health` returned `ok`; `/ready` returned `true`; model information reported EXP-005 classification available and Mode B unavailable. A single safe non-medical `/api/v1/chat` request returned `source=offline_faq`, structured category/safety fields, and the exact mandatory notice “This system is not a medical diagnosis and must not replace a qualified radiologist.”

No OpenAI or Gemini credential was configured or exercised. The preview result demonstrates only the default offline fallback path; provider adapters remain covered solely by injected/mock tests. This Git preview is non-production. It did not merge PR #1, promote a Vercel production deployment, or publish the managed dashboard.

## 2026-08-27 — Raw provider REST parser correction

The original adapters incorrectly expected a shared `output_text` convenience field in raw HTTP JSON. Official raw REST documentation instead identifies OpenAI Responses assistant text in a completed `output` assistant-message content part of type `output_text`, and Gemini Interactions model text in a completed `steps` entry of type `model_output` with a text content part. The adapters now parse only those distinct paths, require exactly one intended text part, and fail closed for absent, empty, non-completed, malformed, or schema-invalid responses. They do not concatenate tool, reasoning, input, or unrelated metadata.

Realistic fixture tests use those two separate raw response shapes and retain Pydantic validation plus post-provider medical-safety filtering. The Gemini Interactions request now includes `store: false`, matching the documented input-only storage control; OpenAI already sends `store: false`. Both settings are documented as request-level stateless preferences only. The full deterministic suite passed with 47 Vitest tests, 60 FastAPI tests, and 8 ML/data tests; no live provider request or credential was used.

GitHub Actions run [`33055061786`](https://github.com/sam300705/neuroinsight-ai/actions/runs/33055061786) passed at raw-parser correction commit `d52f57e8175bb1407cf211f72ed7f42d1650d87e`. The matching non-production Vercel preview `dpl_BfRdZhJCq1eh9VY16KQ2LuGudzg8` is `READY` at `https://neuroinsight-ai-inference-cjbybctjn-sam300705s-projects.vercel.app`: health is `ok`, readiness is `true`, EXP-005 classification is available, and Mode B is unavailable. A safe assistant request returned the deterministic `offline_faq` result with its structured safety fields and exact mandatory disclaimer. No image was submitted, no provider was called, and no production target was changed.

## 2026-08-27 — Local final hardening verification before push

The report endpoint now reconstructs PDF content only from a short-lived, HMAC-authenticated Mode A EXP-005 receipt, with a matching Grad-CAM SHA-256. It fails closed with no `ANALYSIS_RECEIPT_SECRET`, rejects extra client analysis JSON, tampered/wrong-secret/expired tokens, image substitution, replay within the current process, unreportable results, and Mode B issuance. The local replay guard is explicitly bounded and process-local; it is not non-repudiation or distributed replay protection.

Artifact-loader tests now cover malformed checksum configuration, optional original/final redirect host allowlists, HTTPS downgrade rejection, declared and streamed byte limits, truncation, partial-file cleanup, valid cache behavior, and required `image`, `logits`, and `feature_maps` 160-pixel ONNX contract shapes. Provider tests cover raw REST response variants, duplicate text parts, English/Hindi/mixed-language clinical/directive content, prompt-injection-like output, field consistency, local concurrency admission, circuit fallback, and transport disconnects; all use injected providers only.

The completed local matrix passed frozen Node installation, TypeScript, **47** Vitest tests, selected TypeScript coverage at **90.51% lines / 100% functions / 63.63% branches**, production build, 697,072-byte bundle budget, production Node audit, three local production-browser checks, `uv lock --check`, **100** FastAPI tests with enforced critical coverage thresholds, **8** ML/data tests, and `pip-audit -r requirements.lock --strict` with **No known vulnerabilities found**. A CycloneDX SBOM was generated locally. No provider credential/call, image upload, production deployment, dashboard publication, or PR merge occurred. Exact-head CI and preview verification remain pending the push of these changes.

## 2026-08-27 — Static-batch ONNX contract compatibility recovery

The first preview generated from final-hardening commit `a8382b3` was `READY` and served safe health, malformed-upload, Mode B fail-closed, report-integrity, assistant-offline, and CORS responses. It reported Mode A unavailable and logged only `classifier_initialization_failed:category=contract_mismatch:error_type=ClassifierInitializationError`. The newly added strict I/O check was reviewed and found to allow only `None` for the batch dimension, even though a valid ONNX deployment may declare batch one statically. This was a new preview-only compatibility regression, not a checksum bypass, model substitution, or change to the audited EXP-005 160-pixel contract.

The validator now accepts a batch dimension only when it is `1`, `None`, or a non-empty dynamic name. It still requires input `image` with three channels and 160×160 spatial dimensions, output `logits` with four classes, and `feature_maps` with 2048×5×5 dimensions. Focused and full local matrices passed after this correction. The repaired exact-head preview remains pending push and must be verified before considering the branch merge-ready.

GitHub Actions run [`33059030145`](https://github.com/sam300705/neuroinsight-ai/actions/runs/33059030145) passed for recovery commit `083a8ac5778dd62ce1925282eef5024bb18820f7`; its push-triggered duplicate run also passed. The matching Vercel Git preview `dpl_FWsYCEM7BzyiXQHYPq91mvT5RmLY` is `READY` at `https://neuroinsight-ai-inference-odutpz4h5-sam300705s-projects.vercel.app`. `/health` returned `ok`; `/ready` returned `true`; model information reported EXP-005 classification available and Mode B unavailable. Harmless zero-byte upload data returned `422`; a fake report receipt returned `503` because no signing secret is configured; a safe assistant question returned `offline_faq`; CORS allowed the managed dashboard origin and returned `400` with no allow-origin for an unrelated HTTPS origin. No medical image, provider call, secret, dashboard publication, Vercel production deployment, or PR merge occurred.

## 2026-08-27 — Release-safety reconciliation before owner decision

At reviewed PR head `f549122b7b5e003ba236d78e012229b88ea21c33`, GitHub reported an open, clean PR with **50 commits** and **100 changed files**. The final prior CI runs for that head passed. The baseline review-map figures of 44 commits and 142 paths are now explicitly labeled as a historical baseline snapshot rather than final PR totals.

Read-only Vercel project metadata safely confirmed the existing FastAPI project and its latest preview but did not expose deployment environment-variable presence. Therefore production `ANALYSIS_RECEIPT_SECRET` status is recorded as **unknown / owner-controlled**. No secret was inspected, created, rotated, or configured. Current code and tests confirm that, if a future PR release lacks that server-only secret, Mode A classification can remain available when model configuration is intact but no receipt is issued and `/api/v1/report` fails closed with `503`.

The Results page now treats a missing receipt as a deliberate user-visible unavailable state: it displays a clear bilingual explanation, offers no derived-artifact save button, makes no report request/retry, and creates no fabricated download. The corresponding browser transport regression verifies no fetch is attempted for a real result without `analysis_receipt`. This correction applies only to the PR branch; the existing public release and its historical report verification remain distinct.

## 2026-08-27 — Fresh PR-size and branch-protection evidence snapshot

At head `078f6181a0bb61419e9619b4b3b240a52aba399f`, a fresh GitHub pull-request summary returned `state=open`, `merged=false`, `mergeable=true`, **52 commits**, and **154 changed files**. The exact-head workflow `33061136232` (event `pull_request`) and the distinct exact-head workflow `33061132563` (event `push`) each completed successfully. The matching Vercel Git preview `dpl_7cEuz6JTrMFjYobQcdM1pk8SFJFT` remained `READY` at `https://neuroinsight-ai-inference-2l1kbhv25-sam300705s-projects.vercel.app`.

The same fresh GitHub branch-protection read returned `404: Branch not protected` for `main`. No setting was changed. This snapshot is historical after any later documentation-only commit advances the branch; the live PR remains the current source of truth.

## 2026-09-03 — Phase 0 safety and lifecycle implementation

The Phase 0 branch now rejects a solid-red PNG, blank image, and structured full-frame non-scan image with HTTP `422` before the classifier is called. The gate also enforces a 4-megapixel input budget and minimum edge dimension. It is a conservative deterministic plausibility screen only—not a trained MRI-modality or out-of-distribution detector—and passing it does not establish clinical compatibility. Both ONNX and local PyTorch paths now generate Grad-CAM overlays with a longest edge of at most 512 pixels.

Derived artifacts now use deterministic owner/scan/type keys and a database upsert, eliminating the temporary `pending:*` success path and preventing repeated retries from creating new object keys. Deletion validates the authenticated user's storage namespace, requests physical deletion before metadata deletion, treats missing objects as already deleted, and preserves metadata when storage fails so the operation can be retried. Unit tests establish ordering and failure behavior; a provider-backed create/delete check remains required before claiming physical erasure.

The local verification matrix passed **54** Vitest tests with selected-module coverage of **90.37% lines / 100% functions / 70% branches**, **103** FastAPI tests with all critical-module thresholds, and **8** ML/data tests. `mysql2` was upgraded to `3.24.3` and transitive `qs` was pinned to patched `6.16.0`; both production Node and locked Python audits report no known vulnerabilities. TypeScript, production build, and the 476,136-byte initial-bundle gate passed. The local server started, but browser scripts could not run because the Playwright Chromium download repeatedly timed out and no system browser was installed; CI installs its own browser and remains the authoritative browser gate. Exact-head CI, Vercel preview, report-secret configuration, branch protection, and provider-backed storage deletion are recorded separately until verified.

## 2026-09-04 — Phase 0 push and Phase 1A browser/runtime hardening

Phase 0 commit [`2bbf8ab`](https://github.com/sam300705/neuroinsight-ai/commit/2bbf8abd9558357556a52b772cf8e4a64a15100d) passed exact-head GitHub Actions run [`33884318987`](https://github.com/sam300705/neuroinsight-ai/actions/runs/33884318987), including CI-installed browser checks, and its Vercel status completed successfully. The matching preview returned health `200`, readiness `200` with `ready=true`, EXP-005 classification available, and Mode B unavailable. A deliberately bad-checksum PNG preview probe exposed an unhandled Pillow `SyntaxError` as HTTP `500`; the next local change maps that decoder failure to the same safe HTTP `422` corruption response and adds a regression. One lawful exact-head inference and provider-backed physical deletion remain separate gates.

Phase 1A removes filename, derived Grad-CAM, report receipt, and analysis-response persistence from browser storage. Current analysis is memory-only, the legacy local-storage key is removed on startup, and replaced blob preview URLs are revoked. Browser calls now have explicit deadlines without automatic POST retries, successful inference and assistant JSON must satisfy strict runtime schemas, and derived PDF responses have a 15 MB browser-side limit. The local matrix passed **60** Vitest tests with **94.18% lines / 100% functions / 70.58% branches** selected coverage, **104** FastAPI tests with all critical thresholds, **8** ML/data tests, TypeScript, production build, and the **476,109-byte** initial-bundle gate. Commit `5530711` then passed exact-head Actions run `33885744892`; immutable preview `dpl_Dob2Rw2sDdhuRgKw1QPubwrMcJtc` returned health/readiness 200, EXP-005 available, Mode B unavailable, and safe 422 responses for the bad-checksum and solid-red non-MRI probes.

## 2026-09-04 — Phase 1B shared serverless controls

The FastAPI service now has an optional managed Upstash Redis boundary for atomic cross-instance rate-limit counters and short-lived receipt replay claims. Client/endpoint identities and receipt IDs are SHA-256 hashed before key construction. Required mode returns 503 when credentials are absent or a shared operation fails, times out, or returns a malformed value; readiness also fails when required configuration is missing. Credential-free local/test mode retains the bounded process-local fallback and makes no distributed guarantee. The dependency graph pins `upstash-redis` 1.8.0 through the locked Python graph. Injected-client tests cover atomic script arguments, `SET NX EX`, expiry, hashed keys, optional fallback, required failure, replay, and FastAPI readiness/request failure. The complete local matrix passed **60** Vitest tests with selected coverage thresholds, **110** FastAPI/support tests with all critical Python thresholds, **8** ML/data tests, TypeScript, production build, the **476,109-byte** bundle gate, lock consistency, and a Python runtime audit with no known vulnerabilities. A live managed-resource check remains an owner gate.

# Verification Report

**Verification date:** 2026-09-03. The TypeScript dashboard, FastAPI service, reproducibility utilities, and external ONNX service were checked after the real Mode A integration, receipt-protected report route, protected derived-artifact wiring, strict report-PDF/Grad-CAM-PNG artifact pairing, direct Mode B persistence/report rejection, Phase 0 input plausibility and Grad-CAM bounds, retry-safe artifact lifecycle, Express 5 deployment recovery, CSRF protection, ownership-scoped database constraints, cursor-bounded history, and the optional privacy-minimal Research Explanation Assistant. No test or document in this report treats any model output as clinical, diagnostic, patient-level, or externally validated.

| Layer | Command or method | Actual result |
|---|---|---|
| FastAPI service | `uv run --locked --extra test coverage run -m pytest -q tests` | **103 passed**. Coverage includes obvious chromatic/blank/full-frame non-MRI rejection before inference; 4-megapixel and decoder safeguards; 512-pixel Grad-CAM bounds; honest HTTP 503 readiness; report receipt integrity; Model A/Mode B boundaries; model-artifact transport and ONNX contracts; assistant safety; and deterministic PDF contents. The plausibility screen is explicitly heuristic, not a validated MRI/OOD detector. |
| Data utilities | `PYTHONPATH=. pytest -q ml/tests` | **8 passed**. Covers exact-duplicate safeguards, split metadata, NIfTI modality grouping, label linking, AppleDouble exclusion, BRISC path/split leakage safeguards, and strict complete-case full-volume development manifests. |
| Typed application | `pnpm check` | **Passed** with no TypeScript errors. |
| Node and frontend procedures | `pnpm test` | **54 passed**. Coverage includes the 4-megapixel browser preflight, signed report behavior, strict Mode A persistence, deterministic derived-artifact registration contract, owner-scoped download, physical-delete-before-metadata ordering, retry behavior on storage failure, namespace rejection, legacy pending-record denial, and provider storage request contracts. |
| Production dependency audit | `pnpm audit --prod --audit-level=moderate` | **No known vulnerabilities found** after upgrading `mysql2` to `3.24.3` and pinning patched transitive `qs` `6.16.0`. This is a point-in-time package-resolution result, not a substitute for application security review. |
| Ownership and history migration | Reviewed `drizzle/0002_steady_rhodey.sql` and `drizzle/0003_quick_catseye.sql`, duplicate/orphan prechecks, then managed migration | **Passed.** Scan IDs are now unique per owner, artifacts unique per scan/type, foreign keys preserve metadata references, and a `(userId, id)` index supports bounded cursor history. Rollback conditions are documented in `docs/MIGRATIONS.md`. |
| Browser-level flow | `pnpm test:e2e:corrupt-upload`, `pnpm test:e2e:real-inference`, and `pnpm test:e2e:accessibility` | **Passed**. The corruption test proves local signature rejection disables submission. The real-inference test submits one lawful public fixed-split image and verifies the experimental result, exact safety warning, non-medical confidence wording, and protected-save consent control without asserting class correctness. The accessibility test verifies keyboard skip navigation, labelled MRI upload access, and text plus disabled-state error communication. The first and third checks now explicitly acknowledge the Mode A data-use gate and are included in CI against a local production build, not a public inference endpoint. |
| Hindi real-inference flow | `pnpm test:e2e:hindi-real-inference` | **Passed**. The test changes the UI to Hindi, submits one lawful public fixed-split image, and verifies the localized experimental-result and protected-save controls. The exact mandatory English non-diagnostic notice remains visible. |
| Published live integration | `E2E_BASE_URL=https://neuroaiapp-gtbxy6cw.manus.space pnpm test:e2e:real-inference` | **Passed**. The published dashboard reached the CORS-restricted Vercel ONNX service and rendered the real experimental result plus protected-save consent control. This is a connectivity check only and does not assert class correctness, clinical validity, or authenticated artifact retrieval. |
| Authenticated artifact workflow | Signed-in public-browser verification with a lawful public fixed-split image | **Passed**. A derived PDF and real Grad-CAM were saved without retaining the source upload, the report opened through an ownership-gated fresh signed URL, and the temporary record was deleted afterward. Mode B artifact flow remains unavailable. |
| Cross-route accessibility | `pnpm test:e2e:accessibility-routes` | **Passed**. Axe-core WCAG 2 A/AA rules, including colour contrast, pass on overview, analysis, results, history, methodology, performance, limitations, and about. The test also verifies initial keyboard focus reaches the skip-to-main-content link on every route. Browser zoom is not restricted. |
| Production bundle | `pnpm build && pnpm check:bundle` | **Passed**. The current initial `index` JavaScript chunk is `476,136` bytes, below the enforced `768,000`-byte budget. |
| Optional assistant (all mocked) | `PYTHONPATH=. pytest -q backend/tests/test_research_assistant.py backend/tests/test_app.py` and client API regression | **Passed; 48 focused FastAPI tests and 7 client transport tests.** Covers documented raw OpenAI Responses and Gemini Interactions fixtures, exactly-one intended output extraction, missing/empty/non-completed output, irrelevant tool/input metadata, malformed or schema-mismatched JSON, unsafe response fallback, `store: false` for both providers, missing key/model, selected-provider timeout without cross-provider switching, deterministic English/Hindi refusal before provider call, strict input/output schema, and non-sensitive event logging. No live OpenAI/Gemini call, credential, provider enablement, or paid service was used. |
| Critical coverage and Python supply chain | `pnpm test:coverage`; `uv lock --check`; locked Python coverage/audit/SBOM commands | **Passed.** Selected TypeScript modules measured **90.37% lines**, **100% functions**, and **70% branches**. Python critical thresholds passed: receipts 86.76%, assistant 94.68%, ONNX artifact/runtime 85.51%, upload validation 70.08%, and FastAPI routes 85.34%. `uv.lock` remains authoritative for Python 3.12. |
| Production startup recovery | `NODE_ENV=production PORT=3011 node dist/index.js` with a controlled timeout | **Passed locally, then published with owner approval.** The process started after replacing Express 4 wildcard syntax with Express 5-compatible named wildcards and removing the unsafe generic storage proxy. |
| Python package build | `uv build --wheel --out-dir /tmp/neuroinsight-wheelcheck` from `backend/` | **Passed**. The FastAPI service package builds as a standard wheel after explicit setuptools metadata was added. |
| Visual review | Desktop and mobile screenshots of overview, analysis, results, and history | Confirmed readable global disclaimer, responsive navigation, scoped upload guidance, private history empty state, and research-safe no-analysis result state. Detailed findings are in `docs/VERIFICATION_LOG.md`. |

## Actual model and deployment evidence

Mode A uses EXP-005: a ResNet50 head-only classifier trained on the authorised BDNeuro-MRI v7 fixed image-level split after a conservative duplicate-similarity sanitization policy. Its held-out fixed-split image-level test accuracy is `0.8099`, macro-F1 is `0.8080`, and validation-only temperature scaling selected temperature `0.689875` and abstention threshold `0.55`. These figures are not patient-independent, clinical, external, or diagnostic performance evidence.

The Vercel ONNX service passed health, readiness, CORS preflight, corrupt-upload rejection, real inference on one public fixed-split image, and a two-page report generated from its returned real Grad-CAM. That image’s actual predicted class was meningioma despite its glioma label. The system preserves this failure as experimental evidence; browser tests deliberately do not encode it as an expected correct classification. The Grad-CAM is a coarse final-layer classifier attribution, not a tumor boundary.

The corrected non-production preview for commit `c2bb771` is `READY` and reports health `ok`, readiness `true`, EXP-005 classification available, and Mode B unavailable. The raw-parser correction commit `d52f57e8175bb1407cf211f72ed7f42d1650d87e` passed GitHub Actions run [`33055061786`](https://github.com/sam300705/neuroinsight-ai/actions/runs/33055061786). Its Vercel preview is `READY` at `https://neuroinsight-ai-inference-cjbybctjn-sam300705s-projects.vercel.app`: `/health` is `ok`, `/ready` is `true`, EXP-005 classification is available, Mode B is unavailable, and a safe chat request receives `offline_faq` with the exact mandatory disclaimer. The assistant has only injected-provider and offline-path verification; it has no production key and made no live provider request. The existing public managed dashboard and Vercel production target remain unchanged.

The final hardening pass has completed deterministic local and non-production preview verification. The receipt-protected report route is deliberately unavailable in the preview without `ANALYSIS_RECEIPT_SECRET`; CI locks/audits Python dependencies, enforces selected-module coverage, generates a dependency SBOM artifact, and builds/smoke-tests the credential-free FastAPI container. During initial final-hardening preview probing, the strict ONNX I/O assertion treated a static batch-one dimension as a contract mismatch. The validator now allows only static batch `1` or a dynamic/named batch while keeping channels, classes, feature shape, and 160-pixel dimensions fixed. Recovery commit `083a8ac5778dd62ce1925282eef5024bb18820f7` passed GitHub Actions run [`33059030145`](https://github.com/sam300705/neuroinsight-ai/actions/runs/33059030145), and its `READY` preview `https://neuroinsight-ai-inference-odutpz4h5-sam300705s-projects.vercel.app` returned health `ok`, readiness `true`, EXP-005 classification available, Mode B unavailable, a `422` empty-upload rejection, `503` report-integrity-signing rejection, keyless `offline_faq` assistant response, allowed dashboard CORS, and rejected untrusted-origin CORS. This Git preview is not a production release claim.

Mode B remains unavailable. The earlier small, selected-slice 2D segmentation smoke run is not a full-volume, hidden-test, or clinical segmentation model and cannot activate masks, measurements, volume, or 3D geometry as real service outputs.

> **Release status:** Mode A is publicly available only as an image-level, non-clinical academic demonstration with manual review required. Mode B remains unavailable. The public-dashboard CORS configuration, live browser-to-service verification, and user-approved signed-in derived-artifact re-download check are complete.

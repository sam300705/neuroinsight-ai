# Project TODO

- [x] Preserve the academic/research-only positioning and show an always-visible non-diagnostic disclaimer in the global application shell.
- [x] Create a multi-page responsive navigation experience for landing, analyse, results, scan history, methodology, model performance, limitations, and about pages.
- [x] Build a clearly labelled Mode A 2D PNG/JPEG four-class classification workflow for glioma, meningioma, pituitary tumor, and no tumor.
- [x] Build a clearly labelled Mode B glioma-focused volumetric NIfTI segmentation workflow with supported-scope guidance.
- [x] Implement drag-and-drop upload with extension, MIME type, size, and corrupted-file validation plus compatible error states.
- [x] Implement input-quality checks and manual-review warnings for low-quality, incompatible, or out-of-distribution inputs.
- [x] Display the predicted class and explicitly label model confidence as a non-medical model confidence score.
- [x] Implement calibration and uncertainty status with a validation-derived abstention threshold and manual-review warning state.
- [x] Generate real classifier Grad-CAM artifacts with an opacity slider, colour legend, and persistent coarse-attribution disclaimer.
- [x] Render segmentation masks in a 2D slice overlay with the persistent glioma-focused scope disclaimer.
- [x] Calculate segmentation area only with valid spacing metadata and otherwise return pixel count and image-occupancy percentage.
- [x] Calculate volume only from compatible volumetric metadata and multiple compatible slices.
- [x] Generate academic-use PDF reports containing required identifiers, outputs, limitations, and expert-review recommendation.
- [x] Provide an English/Hindi safe contextual chatbot with minimal structured context, treatment refusal, prompt-injection resistance, and an offline FAQ fallback.
- [x] Persist anonymized scan-history metadata and derived artifacts without retaining raw scans by default.
- [x] Add history search, predicted-class filters, reopen-result support, individual deletion, retention controls, and a confirmed delete-all flow.
- [x] Store PDF reports, Grad-CAM overlays, and segmentation artifacts using S3-compatible storage for history re-download. (Mode A persists only derived PDF and real Grad-CAM artifacts through protected, duplicate-safe registration and signed retrieval; no raw upload is stored. A signed-in browser check saved, retrieved, and deleted a temporary public-demo record. Mode B artifacts correctly remain unavailable until a validated full-volume model exists.)
- [x] Add interactive 3D visualization only for compatible segmented volumes, including rotate, zoom, reset, opacity, highlight, loading/error states, and an algorithmic-geometry disclaimer.
- [x] Provide complete English/Hindi language switching across all pages and chatbot responses. (Active-page copy tests cover both languages; the Hindi browser real-inference check verifies localized experimental-result and protected-save controls while the exact required non-diagnostic notice remains visible.)
- [x] Implement accessible keyboard navigation, labelled controls, focus states, contrast, and colour-independent warning communication. (Cross-route WCAG 2 A/AA axe audit passes; browser zoom is enabled, keyboard skip-link behavior passes on all primary routes, and labelled-control/non-colour warning checks pass.)
- [x] Implement backend schemas, secure upload handling, health/readiness, model information, analysis, reporting, chat, and history endpoints.
- [x] Add reproducible data-download guidance, dataset integrity checks, duplicate detection, manifests, patient-level splitting where available, and data-audit outputs.
- [x] Train and evaluate justified classification and segmentation models only after documented dataset access and provenance verification. (Mode A completed EXP-005 with audited data, held-out image-level evaluation, calibration, and deployment. Mode B was correctly retained unavailable because no suitable full-volume validated segmentation model and evaluation evidence exists; the prior 2D smoke model is not activated.)
- [x] Record actual experiments, calibration, model cards, metrics, limitations, and security/privacy evidence without fabrication.
- [x] Add backend, ML, frontend, accessibility, and end-to-end automated tests; visually verify desktop and mobile workflows. (12 FastAPI, 6 ML/data, and 24 web tests pass; saved Playwright checks cover corrupt upload, real external Mode A display, and skip-link/labelled-control/non-colour-warning accessibility; responsive desktop/mobile captures are recorded.)
- [x] Prepare local and production deployment documentation, Docker/Compose assets where justified, CI, and a verified deployment smoke-test procedure. (Documentation and assets are present; package build, FastAPI/ML/web suites, browser checks, and the separate Vercel ONNX smoke path have been verified.)
- [x] Make the academic and non-diagnostic disclaimer persistently visible in the global shell on desktop and mobile layouts.
- [x] Connect the chatbot to current analysis context and add explicit English/Hindi refusals for prompt injection, hidden prompts, diagnosis, and treatment requests.
- [x] Complete source-provenance verification for the classification dataset, or switch to a fully provenance-cleared source, then rerun classification experiments against the approved dataset. (BDNeuro-MRI v7 is documented from its authoritative Mendeley record with CC BY 4.0 evidence, fixed-split audit, duplicate review, image-level limitations, and EXP-005 rerun.)
- [x] Add a real report timestamp and test required report fields, manual-review text, and optional Grad-CAM/segmentation artifact rendering.
- [x] Implement and verify scan-history artifact retrieval and re-download end to end through protected backend procedures and the history page. (A signed-in public-browser check saved only derived Mode A PDF and real Grad-CAM artifacts, opened the report through an ownership-gated fresh signed URL, and removed the temporary record. No original upload was stored.)
- [x] Wire the analysis upload workflow to a backend validation path and surface corruption or incompatibility errors in the user interface. (Client integration and response-mapping unit tests pass. Browser checks now prove a corrupted image is blocked locally and one lawful public fixed-split image reaches real experimental external inference, displays the mandatory non-clinical result, and shows the protected-save consent control.)
- [x] Add end-to-end coverage for a corrupted image or NIfTI upload in the analysis workflow. (Playwright targets the native MRI input, confirms the image-signature error, and confirms that Validate and continue remains disabled before any inference request.)
- [x] Document calibration evidence or its explicit absence for each analysis mode and explain the resulting deployment gate.
- [x] Add verified security and privacy evidence for raw-scan handling, protected history access, artifact boundaries, deletion confirmation, and scope disclaimers.
- [x] Re-audit authoritative public Mode A sources for licence, provenance, citation, format, case identifiers, split integrity, source-confounding risk, and academic-demo suitability.
- [x] Select and document the most defensible permitted Mode A scope, explicitly retaining image-level rather than patient-independent evaluation when case identifiers are unavailable.
- [x] Train, calibrate where justified, and evaluate real reproducible experimental models using the approved source(s), without fabricating clinical validation.
- [x] Connect real local model inference to predictions, model-confidence labels, Grad-CAM, segmentation, measurements, reports, artifact registration, and protected history retrieval. (Mode A real experimental inference, calibrated score, Grad-CAM, reports, artifact registration, and protected-history retrieval are verified end to end. Mode B intentionally returns unavailable rather than fabricating segmentation, measurements, or 3D geometry without a validated full-volume model.)
- [x] Run local end-to-end checks using non-sensitive demonstration files, including corrupt-upload rejection and real derived-artifact retrieval. (Corrupt rejection, real inference, Grad-CAM, and PDF generation are verified locally and from the published dashboard. A signed-in browser check also saved and retrieved the temporary public-demo report through a fresh signed URL before cleanup.)
- [x] Prepare an HTTPS deployment of the FastAPI validation service, verify health/readiness, and set only exact dashboard and localhost CORS origins after the dashboard URL is known. (Vercel ONNX Runtime deployment passed HTTPS health/readiness, CORS preflight, corrupt-upload rejection, and one real public fixed-split image inference check; dashboard now uses its durable HTTPS origin.)
- [x] Create a publish-ready dashboard checkpoint and provide the user with the required project-interface publication handover; do not represent publishing as clinical validation. (Published at https://neuroaiapp-gtbxy6cw.manus.space as a strictly non-clinical academic demonstration.)
- [x] After dashboard publication, add its final public HTTPS origin to the backend CORS allowlist and re-verify preflight without broadening allowed origins. (Exact-origin preflight succeeds; an unrelated origin receives HTTP 400 without an allow-origin header; browser-level real inference from the public dashboard passes.)
- [x] Add explicit browser accessibility coverage for skip-link keyboard behavior, labelled upload controls, and text plus disabled-state communication for corrupted-file warnings. (Saved Playwright check passed.)
- [x] Add documented contrast and keyboard/focus verification across the main dashboard routes, then update the accessibility evidence record. (Saved axe-core/Playwright audit passes on all eight primary routes.)
- [x] Correct the public home-page model-status message so it accurately identifies the verified live Mode A experimental classifier while preserving Mode B’s unavailable status. (Verified on the refreshed public release.)
- [x] Correct the public performance-page status copy so it reports the documented Mode A experimental image-level evidence and keeps Mode B unavailable. (Verified on the refreshed public route with the EXP-005 image-level metrics and explicit Mode B evidence gate.)
- [x] Audit the supplied college presentation and synopsis against the implemented NeuroInsight AI evidence and identify accurate report-alignment gaps. (See `docs/COLLEGE_REPORT_ALIGNMENT.md`.)
- [x] Add only evidence-consistent college-report improvements, preserving the non-clinical, image-level, and Mode B-unavailable boundaries. (Clarified bilingual public copy, corrected the release-report history statement, and added report-ready project wording.)
- [x] Prepare a separate clinician-support research roadmap covering multi-site data, validated full-volume segmentation, clinician evaluation, workflow testing, privacy governance, and applicable regulatory assessment; do not activate clinical claims or functionality without the required evidence and approvals. (Roadmap explained in plain language; no clinical functionality or claims were activated.)
- [x] Research only officially released, de-identified public brain-MRI sources for future data planning; verify provenance, access terms, de-identification statements, and academic-use permissions without collecting hospital or DNA data, downloading restricted data, retraining models, or changing the deployed system. (See `docs/FUTURE_DATA_SOURCING.md`; the release remains unchanged.)
- [x] Evaluate BRISC 2025 as the primary separately gated next-stage research candidate: verify its official Kaggle terms, provenance, classification/segmentation scope, labels, masks, study-identifier availability, and overlap; obtain user-authorized access without accepting terms on the user’s behalf; audit any permitted data; then train and evaluate only after the audit. Keep the public app unchanged unless explicit evidence gates are met. (The authorised local release passed integrity/pairing checks; EXP-006 was a separate non-promoted classifier experiment. Full-volume Mode B remains unavailable.)
- [x] Prepare and attempt the user-requested technical-supervision workflow for BRISC blockers without relying on unverified external advice. (The connection test returned HTTP 429 before any technical recommendation. A privacy-safe, repository-specific future packet is ready in `docs/BRISC_TECHNICAL_SUPERVISION_PACKET.md`; it must be sent only after the owner restores the service quota, and any future recommendation still requires local verification.)
- [x] Prepare a privacy-safe BRISC technical-supervision packet containing the repository URL/branch, actual audit and EXP-006 results, relevant source files, attempted fixes, and precise questions for a future quota-restored consultation; do not include raw images, signed URLs, tokens, or private user data. (Superseded by the documented packet entry immediately below.)
- [x] Prepare a privacy-safe BRISC technical-supervision packet containing the repository URL/branch, actual audit and EXP-006 results, relevant source files, attempted fixes, and precise questions for a future quota-restored consultation; do not include raw images, signed URLs, tokens, or private user data. (See `docs/BRISC_TECHNICAL_SUPERVISION_PACKET.md`; it has not been sent.)
- [x] Measure the optional frontend payload and lazy-load safe routes or visualization components where it reduces the initial bundle without changing accessibility, bilingual copy, or the current Mode A/Mode B boundaries. (Route-level code splitting reduced the initial JavaScript chunk from 1,407.37 kB to 682.51 kB, a measured 51.50% reduction. TypeScript, 24 Vitest tests, production build, and cross-route WCAG/skip-link browser audit pass; see `docs/FRONTEND_PERFORMANCE.md`.)
- [x] Remove the dormant Three.js renderer from the always-unavailable Mode B results path while retaining an accessible, bilingual explanation that 3D geometry remains unavailable pending validated full-volume segmentation. (The Results chunk fell from 656.31 kB to 41.16 kB, a measured 93.72% reduction; TypeScript, 24 Vitest tests, build, and cross-route WCAG/skip-link audit pass.)
- [x] Reconcile the user-supplied overnight brief with the verified project state, correcting only genuinely stale capability statements and preserving the completed protected-artifact evidence. (Corrected `PROJECT_STATUS.md`, `docs/OPEN_GATES.md`, `docs/EXTERNAL_PREREQUISITES.md`, `docs/SECURITY_PRIVACY_EVIDENCE.md`, and stale public test count.)
- [x] Finish the BRISC integrity, mask-pairing, duplicate, and leakage-resistant split audit; document exact results and keep raw data outside the repository. (See `docs/BRISC_AUDIT.md`; the local raw data and generated manifests remain outside Git.)
- [x] Add a canonical capability manifest and consistency checks only after auditing the current implementation, deployment evidence, and user-facing copy. (Added `docs/CAPABILITY_MANIFEST.md` and a passing regression test.)
- [x] Prepare a morning checklist that records only unavoidable manual gates, including any unavailable technical-supervision quota and model-promotion requirements. (Added `docs/MORNING_SETUP_CHECKLIST.md`.)
- [x] Assess local training resources and run only evidence-gated BRISC experiments that do not modify the live service; retain Mode B as unavailable unless its separate release gate is met. (No GPU was available. A separate CPU-only EXP-006 ResNet18 head-only run used the sanitised BRISC manifests and was not promoted because its official image-level test result was lower than EXP-005.)
- [x] Verify the remote GitHub baseline and create a dedicated feature branch for the requested safe NeuroInsight AI improvements. (Remote `github/main` is at `26498b5`; local verified checkpoints were ahead by seven commits. Created `feature/overnight-safe-improvements` from the local improvement state.)
- [x] Reconcile or implement safe unblocked improvements for protected History artifacts, lifecycle documentation, capability/status records, README, checklist, CI, lazy loading, authorization coverage, and the unactivated case-disjoint Mode B research pipeline. (Added ownership-lifecycle helpers/tests, case-disjoint full-volume manifest preparation/tests, CI safeguards, and synchronized status/evidence records; the existing README, checklist, manifest, protected-history workflow, and bundle reductions are included on the feature branch.)
- [x] Run the exact repository verification suite, create logical commits, push the feature branch, and open a GitHub pull request with tested evidence and remaining manual gates. (Branch `feature/overnight-safe-improvements` is pushed at `ce8945cd7e55a52f38f4707be972b95693bd47c4`; PR #1 is open at `https://github.com/sam300705/neuroinsight-ai/pull/1`.)
- [x] Inspect the reported PR #1 GitHub Actions failure, reconcile pnpm version sources without weakening reproducibility, and verify the actual workflow result after a focused branch update. (Removed the duplicate `pnpm/action-setup` version, moved supported overrides/patch configuration to `pnpm-workspace.yaml`, made inference health an explicit opt-in smoke test, corrected the invalid PDF image test fixture, and verified PR workflow run `32597451692` completed successfully.)
- [x] Audit and remove only accidentally tracked generated caches, checkpoints, temporary outputs, raw data, or secrets; strengthen ignore rules and repository guards without deleting legitimate source or published model metadata. (Removed tracked Python bytecode caches; `.gitignore` and CI now block cache, secret, raw-data, NIfTI/DICOM, and local-checkpoint patterns.)
- [x] Perform a factual full-system gap review and implement only remaining safe, evidence-gated improvements to security, upload handling, scientific transparency, accessibility, performance, and full-volume Mode B preparation while keeping the deployed research boundaries intact. (Added a conservative HTTP header policy, bounded dashboard request parsers, server timeouts, graceful shutdown, focused regression tests, and `docs/SYSTEM_GAP_ANALYSIS.md`; the existing authorization, upload, scientific-scope, accessibility, performance, and Mode B preparation evidence remains intact.)
- [x] Add missing repository governance: security-reporting policy, contribution guide, community conduct standard, neutral CODEOWNERS example, pull-request template, issue forms, Dependabot configuration, and documented branch-protection recommendations without inventing contacts, approvals, or certifications. (Added `SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `.github/` templates/configuration, and `docs/REPOSITORY_GOVERNANCE.md`.)
- [x] Push verified PR #1 updates and report the exact GitHub Actions/deployment-check state, test results, remaining manual actions, environment-variable names, costs, readiness assessment, and merge recommendation. (Pushed commits through `32fc9aeb90c1b25f243201cb752542ee0fe02234`; GitHub Actions run `32597964594` completed successfully. No paid resources, new secrets, or deployment configuration changes were introduced.)
- [x] Improve Python dependency caching in CI without weakening frozen Node installation, generated-artifact hygiene, or backend/ML regression coverage. (Enabled `actions/setup-python` pip caching keyed by `backend/requirements.txt` and removed unused PyTorch, torchvision, and scikit-learn installation from the test job after auditing every ML test import. Local verification still passed TypeScript, 31 Vitest tests, YAML formatting, 12 FastAPI tests, and 8 ML/data tests.)
- [x] Perform a second codebase, public-UI, deployment, dependency, test, artifact-lifecycle, and operational audit; fix every independently actionable defect found without changing the approved Mode A model or enabling Mode B. (Found and fixed direct Mode B persistence/report bypasses, upload resource-limit gaps, stale warning wording, and critical/high production dependency findings.)
- [x] Re-audit real Mode A safeguards, upload/error handling, report fields, ownership isolation, and environment-failure behavior; add only meaningful regression coverage for genuine gaps. (Mode A remains the only persistable/reportable analysis mode; signed ownership checks remain unchanged and valid Mode A PDF/report tests pass.)
- [x] Prevent direct authenticated API callers from persisting or registering unavailable Mode B segmentation/3D artifacts, with explicit fail-closed validation tests. (Server validation now rejects unavailable segmentation scan records and artifact registrations; direct tests preserve valid Mode A flows.)
- [x] Harden inference-upload resource handling with early content-length rejection, bounded stream reads, image pixel limits, decompression-bomb rejection, and regression tests without changing valid Mode A behavior. (Added a 12-megapixel decoder limit, bounded body reads, early size checks, and explicit image-mode validation with FastAPI tests.)
- [x] Prevent public inference-service clients from generating Mode B segmentation reports or accepting synthetic segmentation overlays while Mode B is unavailable, with explicit API regression tests. (The report endpoint now rejects segmentation analysis requests; Mode A PDF generation remains tested.)
- [x] Reconcile environment and deployment documentation with actual inference runtime variables, fixed upload limits, Mode B fail-closed reports, and the absence of a configured service token. (Updated `docs/ENVIRONMENT.md` and `docs/DEPLOYMENT.md`.)
- [x] Align client-side image preflight with the backend’s 12-megapixel safety limit and add a focused regression test for the accessible early error. (UploadDropzone rejects over-budget images before submission and exposes an accessible error.)
- [x] Remediate independently actionable critical/high production dependency findings with targeted compatible upgrades, then rerun type, test, build, and audit checks. (Updated AWS SDK, tRPC, nanoid, Axios, Drizzle, and Express within supported release lines; final production audit reports no known vulnerabilities.)
- [x] Remove the unreferenced Recharts wrapper and dependency to eliminate its remaining high-severity lodash finding without changing any rendered NeuroInsight interface. (Confirmed no application imports, removed the wrapper/dependency, and reverified the build.)
- [x] Remove the unreferenced Streamdown-only component showcase and dependency to eliminate the final high-severity production audit finding without changing the published routes. (Confirmed the showcase was unreachable, removed it and its private chat wrapper/dependency, and obtained a clean production audit.)
- [x] Synchronize public test, status, and gap-analysis records with the newly added safeguards, 34 web tests, dependency remediation, and clean high-severity production audit. (Updated `PROJECT_STATUS.md`, `docs/TEST_REPORT.md`, and `docs/SYSTEM_GAP_ANALYSIS.md`.)
- [x] Restore the exact mandatory visible disclaimer, “This system is not a medical diagnosis and must not replace a qualified radiologist.”, across the public research-warning components and add a copy regression assertion. (The shared English warning and Hindi-mode mandatory English sentence now contain the exact phrase; focused regression and local visual verification pass.)
- [x] Re-assess available authorized full-volume resources and current synthetic Mode B preparation; complete only safe pipeline or fixture work that does not fabricate segmentation output or activate Mode B. (Verified the Task01 metadata and existing complete-case manifest/training preparation without reading raw images, training, or exposing a segmentation result. Full-volume release evidence remains unavailable.)
- [x] Re-measure and improve independently actionable UI, accessibility, security, performance, and operational-readiness gaps while preserving exact non-clinical language and bilingual scope messaging. (Local overview and analysis visual review pass; the visible exact disclaimer is restored; the initial bundle remains approximately 682.61 kB with the known non-blocking Vite warning.)
- [x] Run a second full local/public/CI verification pass, push all safe fixes to PR #1, and classify every remaining item as verified or blocked by a named owner-controlled resource or approval. (Local verification passed 34 web tests, 15 FastAPI tests, 8 ML/data tests, production build, hygiene diff check, and a clean high-severity production dependency audit. The Mode A/Mode B visual review preserved the exact non-diagnostic warning and unavailable Mode B card. PR workflow `32617679927` passed on second-pass code; remaining gates are owner-controlled data/compute, research release review, technical-supervision quota, and GitHub protection settings.)
- [x] Audit and, where API authorization permits, apply proportional branch protection to `main` using the existing verification workflow without blocking the current open PR or inventing reviewer requirements. (Audited as unprotected; the available personal-repository GitHub API authorization rejected the documented restriction payload. The exact owner Settings action is recorded in `MANUAL_GATES.md`.)
- [x] Reconfirm locally authorized full-volume dataset metadata, current no-GPU resource status, and reproducible Mode B preparation boundaries without loading images, accepting external terms, spending money, training, or activating unavailable outputs. (Task01 manifest reports 484 training and 266 test entries; the case-disjoint preparer is available; no GPU is present and only 1.8 GiB memory was available. No image data was read, no training ran, and Mode B remains unavailable.)
- [x] Document the exact owner-controlled prerequisites for any future full-volume study, technical-supervision consultation, or Mode B release review after the protection/readiness audit. (Added `MANUAL_GATES.md` with data, compute, publication, storage, GitHub, quota, and legal gates.)
- [x] Restore production startup by resolving the Express 5 route-pattern incompatibility in the built-in Manus storage proxy without weakening artifact authorization. (Replaced legacy catch-all routes with named Express 5 wildcards, removed the unauthenticated proxy, and verified a built production server starts locally. Publication remains owner-gated.)
- [x] Record a fresh branch, deployment, test, dependency, and capability baseline against the latest product-hardening acceptance criteria before any broad refactor. (Recorded branch `feature/overnight-safe-improvements`, deployment failure cause, test/build baseline, clean production audit, and canonical capability boundaries.)
- [x] Remove unavailable Mode B from the active public upload workflow while retaining a clear disabled research-roadmap explanation and no NIfTI prompt. (Mode A is the only upload path; Mode B is a non-interactive roadmap card and server endpoints remain fail-closed.)
- [x] Add the safe Learn–Explore–Analyse–Understand–Save–Review research-sandbox journey, sample-gallery infrastructure without image files, and truthful capability/version evidence to the public product experience. (Added a licensed-sample empty manifest, disabled sample CTA, journey, version/evidence panel, capability card, and responsible-engineering section.)
- [x] Audit and implement feasible origin/CSRF, internal storage-proxy, report-integrity, request-correlation, structured-logging, and security-header safeguards without fabricating platform capabilities. (Implemented CSRF, removed the generic proxy, added CSP, bounded request IDs, and documented report-integrity/rate-limit gaps as manual gates because no shared receipt secret or distributed limiter exists.)
- [x] Remove the unauthenticated generic `/manus-storage/*` proxy, stop returning stored keys/URLs in history and registration responses, and retain only ownership-scoped fresh signed artifact downloads. (Generic proxy module is deleted; history/registration no longer expose storage key/URL values.)
- [x] Add and test a fail-closed same-origin CSRF guard for cookie-authenticated tRPC mutations while preserving legitimate same-origin and local development requests. (Four unit tests pass.)
- [x] Sanitize and length-limit inference request IDs, preserve a safe generated correlation ID in responses, and add regression coverage for malformed or oversized values. (FastAPI now accepts a 128-character safe token pattern or generates a UUID; regression test passes.)
- [x] Add privacy, acceptable-use, model-card, data-lifecycle, operations, threat-model, and manual-gates documentation that accurately separates implemented controls from owner/legal/platform requirements. (Added public responsible-use page plus `MANUAL_GATES.md` and six scoped engineering documents.)
- [x] Publish owner-approved commit `ee2d3f6` and verify the live dashboard, authentication boundary, real Mode A integration, and Mode B disabled state after the managed deployment completes. (Checkpoint `409f8a70` published successfully. The public dashboard loads the recovery/product update and exact warning; unauthenticated History shows only the sign-in gate; a synthetic non-sensitive PNG received a real public Mode A `classification`/`complete` EXP-005 response with manual review recommended; public model-info reports segmentation `unconfigured`/`unavailable`.)
- [x] Reconcile the owner-approved published checkpoint with the remote PR branch, then record the exact latest public verification evidence without implying a merge. (The owner-approved public recovery remains checkpoint `409f8a70`; the remote PR branch now reaches `0861734` and GitHub Actions workflow-dispatch run `32990002824` passed at that head. PR #1 explicitly labels the post-recovery history/DB, limiter, contract, CI, and documentation changes as verified but not republished. No merge was performed.)
- [x] Audit and strengthen database ownership and artifact uniqueness invariants with reversible documented migrations, collision/idempotency tests, and no cross-user upsert path. (Applied reviewed migrations `0002` and `0003` after duplicate/orphan checks. Scan IDs are unique per owner; artifacts are unique per scan/type; foreign keys and static migration regression tests enforce the contract.)
- [x] Design and implement bounded, indexed, ownership-scoped history querying with accessible pagination/filtering behavior and focused regression coverage where the existing data model supports it. (History now returns a newest-first 20-item cursor page with server-side search/predicted-class filters, one batched artifact query, and accessible next/previous controls; `(userId,id)` index migration is applied.)
- [x] Add safe public-inference abuse, report-provenance, quality-heuristic, and structured-observability improvements that work without paid/distributed infrastructure; document the remaining infrastructure gates precisely. (Added bounded request IDs and 20/minute process-local burst limits with `429`/`Retry-After`; uploads already enforce byte, pixel, decoder, and channel safety. Distributed rate limiting and cryptographic report signing remain documented infrastructure gates.)
- [x] Add evidence-only Mode A model-contract drift tests and improve the unactivated Mode B research preparation without training, activating, or claiming clinical/full-volume validation. (Added generated-fixture tests for EXP-005 labels, public mapping, preprocessing size/normalization, metadata and calibration validation. Mode B remains preparation-only.)
- [x] Tighten practical CI quality gates, reachability/dead-code checks, bundle-budget evidence, and documentation consistency while preserving deterministic offline CI. (CI now runs clean production dependency audit and a 750 KiB initial-bundle guard after build. A global Prettier check was evaluated but withheld because it correctly exposed 126 pre-existing style violations; a mass formatting rewrite was not safe for this change set.)
- [x] Add the missing architecture and recruiter engineering-story documentation, then reconcile all public status/test/capability records with implementation and live evidence. (Added Mermaid architecture/runtime/ML/privacy diagrams and `docs/ENGINEERING_STORY.md`; refreshed status, security, migration, and verification records.)
- [x] Correct the final local History-copy discrepancy found during visual verification: remove anonymous, durable-link, and Mode B artifact claims; add bilingual ownership/fresh-link wording and retry/pagination labels; and lock it with a regression test. (The local History UI now states account-linked pseudonymous Mode A metadata, fresh ownership-checked PDF/Grad-CAM downloads, and unavailable Mode B masks/3D artifacts. The updated bilingual copy test passes.)
- [x] Perform a fresh independent baseline audit of `main`, PR #1, GitHub Actions, deployment/preview evidence, repository structure, migrations, tests, and all status/manual-gate records before further changes. (Confirmed `github/main` at `26498b5`, PR #1 open/mergeable at `3252cb9`, managed dashboard recovery checkpoint `409f8a70`, Vercel production target on `main`, and a separate branch preview. Verified applied owner/scan and artifact indexes through a read-only database query.)
- [x] Reconcile every identified documentation/deployment contradiction with implementation and verifiable GitHub/public evidence, preserving historical experiment records. (Corrected stale anonymous-history wording, historical test totals, public-recovery/manual-gate language, CORS guidance, and branch-versus-public release distinction across README, handover, verification, capability, deployment, security, and college-report records.)
- [x] Audit and safely improve deterministic browser CI, Python dependency consistency, migration/history lifecycle coverage, security/input controls, dead-code hygiene, and README portfolio clarity where gaps are actually found. (Repaired acknowledgement-aware browser scripts and added local production-build CI coverage; aligned Python runtime/test dependency roles; added bounded limiter cleanup/cap; added `docs/REACHABILITY_AUDIT.md`; and removed unreachable Maps/Mode B/Three.js code.)
- [x] Execute the complete local/browser/preview verification set and adversarial review, then push only verified changes and obtain a GitHub Actions result at the final PR head without publishing production. (The 45/25/8 suite, production startup probe, desktop/mobile visual checks, tracked-artifact scan, current-head Actions run `33045574801`, and Vercel `READY` preview were completed. The branch preview remains safely Mode A unavailable under the documented artifact gate; production was not changed.)
- [x] Prevent an unavailable or malformed external EXP-005 artifact from crashing the Vercel preview at startup; fail closed to an unavailable Mode A response and add regression coverage. (The new FastAPI lifespan regression passed locally. Vercel preview deployment `dpl_JDAhZbVBK4xU9gPpcxmmM9YdfZhH` for `8d5d02f` returns structured `200` model information with classification and segmentation unavailable instead of the prior `500 FUNCTION_INVOCATION_FAILED`; the existing production endpoint remains unchanged.)
- [x] Bound process-local limiter key retention after expired clients and verify its cleanup behavior without representing it as distributed protection. (Expired keys are pruned during subsequent requests and active keys are capped at 2,048; three focused tests pass. The limiter is still precisely documented as process-local rather than distributed.)
- [x] Correct stale anonymous-history and obsolete test/deployment counts in public handover, README, capability, verification, and manual-gate records using the current repository/public/preview evidence. (Updated account-linked pseudonymous Mode A-only history wording, fresh signed URL lifecycle limits, and the approved public-recovery/branch-preview distinction; subsequent final audit regressions are recorded at 45/25/8.)
- [x] Remove dormant Mode B mask/measurement/3D rendering paths and unused Three.js dependencies that contradict the intended fail-closed public capability boundary, then verify no reachable imports remain. (Removed inactive `SegmentationOverlay`, `Volume3DViewer`, and Maps component paths; removed `three`, `@types/three`, and `@types/google.maps`; Results now renders only the explicit unavailable Mode B explanation; source scan and build pass.)
- [x] Return the existing Mode B unavailable response before application-level file reads or decode validation on both segmentation entry points, with a regression proving those paths cannot process public uploads. (Both `analyze` segmentation mode and `/segment` close the parsed upload and return `unavailable` before application-level read/decode; the regression replaces the reader with an error to enforce this.)
- [x] Reconfirm the exact latest PR head, mergeability, latest Actions, Vercel preview, `main`, managed-dashboard release, and Vercel production target; correct the PR description only if it is stale. (PR #1 remains open and mergeable at `f17386d` over `main` `26498b5`; Actions `33043751043` passed; Vercel preview `dpl_BkMcnXKW4iVfuEvWh6KXh172fesR` is `READY`; Vercel production remains `26498b5`; and the public dashboard remains the separate owner-approved checkpoint `409f8a70`.)
- [x] Perform the final static authorization, signed-artifact lifecycle, claim-language, EXP-005/EXP-006, migration-state, upload-abuse, dependency, and repository-hygiene audit; document every genuine remaining owner gate once. (Verified owner filters and fresh signer gating, no storage delete primitive, Mode A-only validation, exact image-level EXP-005/negative EXP-006 wording, reviewed `0002`/`0003`, public upload/report bounds, clean runtime dependency parity, and no prohibited tracked files. Recorded the external artifact-initialization and proxy-address boundaries.)
- [x] Re-attempt proportional `main` branch-protection inspection without weakening requirements, then record verified state or exact owner UI instructions. (Read-only GitHub API again returned `404 Branch not protected`; `MANUAL_GATES.md` and the morning checklist preserve exact owner UI steps.)
- [x] Run and record the attachment-required final deterministic suite, browser checks, current-head Vercel preview validation, and multi-discipline pre-merge review; push only needed evidence updates without merging or publishing. (Static/data lifecycle, security/privacy, ML safety, CI/dependency, and product/UX checks were completed; all newly found safe defects were fixed and regression-tested. Current branch preview `dpl_8XSnrDvFQSmTDivXjrMH8MqdAhKh` is `READY` and fails closed rather than crashing.)
- [x] Diagnose the exact EXP-005 preview startup failure from metadata, calibration, contract, artifact transport, and runtime evidence without downloading/replacing the ONNX checkpoint or weakening integrity checks. (The exact cause was a 224-pixel hard contract conflicting with EXP-005’s audited/metadata 160-pixel preprocessing. Metadata and calibration checksums were confirmed; the 94 MB ONNX checkpoint was not retrieved or replaced.)
- [x] Correct only a verified immutable EXP-005 contract/configuration mismatch, add safe internal error categories, and prove non-production Mode A initialization, health, readiness, and one permitted sample inference before any merge decision. (Commit `c2bb771` changes only the erroneous contract to 160 pixels and adds internal categories. Its Vercel preview is `READY`, reports Mode A available/Mode B unavailable, passes health/ready, and completed exactly one checksum-matched sanitized non-test public training image as an integration—not performance—check.)
- [x] Implement a server-side provider abstraction for an optional OpenAI or Gemini research-explanation layer that defaults to the existing offline FAQ when no configured provider is available. (Exactly one provider can be selected only by server-side `AI_PROVIDER`; a missing key/model disables the cloud path without any outbound call.)
- [x] Enforce minimum de-identified structured context, server-side medical/prompt-injection refusal rules, bounded inputs/outputs/timeouts/rate limits, validated structured outputs, and non-sensitive observability for the assistant. (The strict FastAPI schema forbids raw/identity fields; provider responses are Pydantic-validated and post-filtered; the route has its own 10/min process-local limiter and logs only provider/outcome/category/latency/request ID.)
- [x] Build a bilingual Research Explanation Assistant UX and result-explanation action that keeps deterministic EXP-005 output authoritative and preserves the offline FAQ fallback. (The browser sends only an allowlisted derived payload and displays actual offline/server-side source; it cannot send filename, scan ID, preview, Grad-CAM binary, full analysis state, account data, or credentials.)
- [x] Add provider-mocked safety and privacy regression coverage, update architecture/privacy/capability/environment/test documentation, and complete full non-production verification without merging or publishing. (Final head `a1c8fb5` passed Actions `33048397462`; the matching Vercel Git preview is `READY`, preserves EXP-005 Mode A availability/Mode B unavailability, and returns the offline assistant response with the exact mandatory disclaimer. No provider key/call, PR merge, dashboard publication, or Vercel production deployment occurred.)
- [x] Prevent authenticated artifact registration type confusion by mapping each Mode A artifact type to its only valid content type and rejecting JSON/3D payload MIME types, with regression coverage. (The strict discriminated contract accepts only report/PDF and Grad-CAM/PNG; new regression cases reject cross-type PDF/PNG, JSON, and glTF MIME values.)
- [x] Correct the stale anonymous-history wording in `docs/ENVIRONMENT.md` and expand `docs/MORNING_SETUP_CHECKLIST.md` to include every genuine current owner-controlled gate without implying a required public release. (Environment documentation now says account-linked pseudonymous history; the checklist covers branch protection, physical deletion, shared-inference hardening, and future public-release authorization.)
- [x] Remove the unreferenced direct `add` development dependency after confirming no package script or source file invokes it, then re-run the frozen-install, type, test, build, and audit checks. (Removed the direct development dependency and its lockfile records; the full frozen-install, check, test, build, bundle, and audit suite passed.)
- [x] Add and test a narrow FastAPI declared-size guard for report JSON requests so oversized or malformed `Content-Length` values fail before request-model parsing, without changing valid Mode A report behavior. (Malformed and over-15-MB declared report requests return a safe `422` and request ID before model parsing; valid Mode A PDF regression remains passing.)
- [x] Enforce classification-only history listing so legacy database enum values or crafted list filters cannot expose any historical Mode B artifact, measurement, or 3D metadata through the generic history API. (The strict query contract rejects a `mode` parameter and always filters Mode A classification records; focused policy tests pass.)
- [x] Record the final Vercel preview’s controlled Mode A artifact-initialization failure as a precise owner verification gate, without downloading or replacing the 94 MB ONNX checkpoint or changing production. (Recorded in `MANUAL_GATES.md` and `docs/VERIFICATION_LOG.md`, with header checks and in-memory metadata/calibration checksum checks only.)
- [x] Document the process-local limiter’s direct `request.client.host` keying and unverified proxy-address assumption without trusting client-supplied forwarding headers or overstating production-wide coverage. (Security evidence now identifies framework-provided host keying, rejects untrusted forwarding headers, and retains the explicit non-distributed limitation.)
- [x] Verify the current official raw OpenAI Responses and Gemini Interactions REST response contracts, then correct provider-specific extraction without using SDK-only convenience fields. (Completed; detailed implementation evidence is retained below.)
- [x] Make the Gemini Interactions request explicitly stateless with `store: false` where the documented contract supports it, while preserving the existing de-identified allowlist and offline-only default. (Completed; detailed implementation evidence is retained below.)
- [x] Replace artificial provider fixtures with realistic raw REST response fixtures and regressions for malformed, missing, unsafe, and schema-mismatched provider outputs. (Completed; detailed implementation evidence is retained below.)
- [x] Re-run the complete deterministic, browser, and non-production preview verification, update PR #1 evidence/description with actual results, and retain all production/provider owner gates. (The raw-parser correction passed 47 Vitest, 60 FastAPI, and 8 ML/data tests plus browser/build/bundle/audit checks; its `READY` Vercel preview preserves EXP-005 availability, Mode B unavailability, and the keyless offline assistant. PR #1 now carries the current parser/privacy, test, preview, and owner-gate evidence. No provider key/call, merge, production promotion, or dashboard publication occurred.)
- [x] Verify the current official raw OpenAI Responses and Gemini Interactions REST response contracts, then correct provider-specific extraction without using SDK-only convenience fields. (OpenAI extracts only one completed assistant `output/message/content` part of type `output_text`; Gemini extracts only one completed `steps/model_output/content` part of type `text`.)
- [x] Make the Gemini Interactions request explicitly stateless with `store: false` where the documented contract supports it, while preserving the existing de-identified allowlist and offline-only default. (OpenAI and Gemini payloads both include `store: false`; this is documented as a request-level setting only.)
- [x] Replace artificial provider fixtures with realistic raw REST response fixtures and regressions for malformed, missing, unsafe, and schema-mismatched provider outputs. (Dedicated OpenAI Responses and Gemini Interactions raw-JSON fixtures, exact-path parsers, and 48 focused safety tests pass with mocked transports.)
- [x] Audit the exact-head report endpoint, receipt/signing feasibility, artifact loader, model contract, Python dependency sources, CI workflow, deployment files, and changed-file risk groups before modifying high-risk paths. (Completed; detailed implementation evidence is retained below.)
- [x] Implement a fail-closed, short-lived signed Mode A analysis receipt for report generation; reject missing/invalid/expired/tampered/wrong-secret/replayed receipts, Mode B analysis, and segmentation overlays without committing a secret or enabling production signing. (Completed; detailed implementation evidence is retained below.)
- [x] Harden verified model-artifact downloads with per-artifact byte limits, URL/redirect HTTPS and host controls, streamed and declared-length checks, partial-file cleanup, pre-download SHA validation, and startup-time ONNX I/O contract validation. (Completed; detailed implementation evidence is retained below.)
- [x] Strengthen deterministic provider output safety for English, Hindi, and mixed-language clinical advice, category/flag consistency, prompt-injection variations, bounded concurrency, and fail-closed provider availability behavior without any live provider request. (Completed; detailed implementation evidence is retained below.)
- [x] Add reproducible Python dependency constraints/lock consistency, a non-claiming Python vulnerability audit in CI, deliberate Python-version documentation, and feasible credential-free backend build/smoke coverage without modifying production hosting. (Completed; detailed implementation evidence is retained below.)
- [x] Add meaningful Python/TypeScript coverage reporting and critical-module thresholds, including adversarial JSON, Unicode, duplicate-text, disconnect/timeout, concurrency, ownership, and deletion coverage where code-owned and feasible. (Completed; detailed implementation evidence is retained below.)
- [x] Audit the exact-head report endpoint, receipt/signing feasibility, artifact loader, model contract, Python dependency sources, CI workflow, deployment files, and changed-file risk groups before modifying high-risk paths. (Detailed review covered the FastAPI/report/artifact contract, tRPC ownership/storage/migrations, CI/Docker/dependency paths, and a classified inventory of all GitHub-reported 142 PR files; the review map explicitly does not claim every changed line was semantically audited.)
- [x] Implement a fail-closed, short-lived signed Mode A analysis receipt for report generation; reject missing/invalid/expired/tampered/wrong-secret/replayed receipts, Mode B analysis, and segmentation overlays without committing a secret or enabling production signing. (HMAC receipt claims bind reportable EXP-005 analysis and Grad-CAM digest; report API reconstructs claims only. Secret absence returns 503 and no environment value is committed.)
- [x] Harden verified model-artifact downloads with per-artifact byte limits, URL/redirect HTTPS and host controls, streamed and declared-length checks, partial-file cleanup, pre-download SHA validation, and startup-time ONNX I/O contract validation. (Limits are 128 MiB model/1 MiB JSON, with optional explicit host allowlist and fixed 160-pixel I/O checks.)
- [x] Strengthen deterministic provider output safety for English, Hindi, and mixed-language clinical advice, category/flag consistency, prompt-injection variations, bounded concurrency, and fail-closed provider availability behavior without any live provider request. (All tests use injected transports; refusal/fallback occurs before or after the bounded local provider boundary as appropriate.)
- [x] Add reproducible Python dependency constraints/lock consistency, a non-claiming Python vulnerability audit in CI, deliberate Python-version documentation, and feasible credential-free backend build/smoke coverage without modifying production hosting. (Python 3.12, uv lock/check, constrained Docker install, runtime requirements audit, SBOM, and CI container smoke are configured.)
- [x] Add meaningful Python/TypeScript coverage reporting and critical-module thresholds, including adversarial JSON, Unicode, duplicate-text, disconnect/timeout, concurrency, ownership, and deletion coverage where code-owned and feasible. (The local matrix measured/enforced selected critical thresholds and passing adversarial/provider/receipt cases.)
- [x] Verify the exact PR preview for health/readiness/model info, malformed/oversized upload rejection, Mode B rejection, offline assistant behavior, report-integrity rejection, and allowed/rejected CORS origins using no patient data. (Recovery preview `dpl_FWsYCEM7BzyiXQHYPq91mvT5RmLY` is `READY`: EXP-005 is available at readiness true; Mode B is unavailable; zero-byte malformed upload is 422; report signing is 503 without secret; assistant is offline FAQ; dashboard CORS is allowed and unrelated origin rejected.)
- [x] Correct the strict ONNX I/O validator so it accepts the audited fixed 160-pixel contract with either a dynamic or static batch dimension, then re-run exact-head CI and non-production preview verification. (Static batch one is now accepted without relaxing fixed channels/classes/spatial dimensions; recovery commit `083a8ac` passed Actions `33059030145` and its matching preview restored Mode A availability.)
- [x] Refresh current PR #1 head, merge state, commit/file totals, latest Actions/preview, main protection, and owner-controlled production report-signing status without exposing or changing secrets. (At reviewed head `f549122`, PR #1 was open/clean with 50 commits and 100 files; main returned 404 “Branch not protected”; authorized Vercel metadata did not expose environment presence, so production receipt signing remains unknown/owner-controlled.)
- [x] Reconcile `PR_REVIEW_MAP`, `PUBLIC_HANDOVER`, report-capability claims, and branch-versus-public-release wording with actual current verification totals and the two valid owner report-release choices. (Baseline and current snapshots are distinct; branch totals are 48/100/8; documentation now identifies preserving reports with a secret or intentionally disabling reports with explicit UI/product copy as the only two choices.)
- [x] Verify fail-closed report-signing and clean dashboard behavior when a receipt is absent using safe local/non-production tests, without configuring a secret or creating a false report download. (No-receipt helper/UI behavior returns a clear message, makes no fetch/retry, offers no save button, and creates no download; receipt API rejects unavailable signing with 503.)
- [x] Re-run the complete deterministic suite and safe exact-head preview checks after reconciliation, then update the PR evidence with actual current facts only. (48 Vitest, 100 FastAPI, and 8 ML/data tests passed with coverage/build/audit/browser gates; reconciliation-head Actions `33060798532` passed and matching preview `dpl_A82i5GeENkL5ywmAp8E1kaZ7FyPQ` was READY with EXP-005 available, Mode B unavailable, offline assistant fallback, and report 503 without signing.)
- [x] Perform fresh read-only PR #1 size, exact-head, branch-protection, CI, and Vercel status checks for the final evidence-only reconciliation. (At `078f618`, GitHub returned open/unmerged/mergeable, 52 commits, 154 files; `main` fresh protection read returned 404 Branch not protected; pull-request and push workflows were separately successful; matching Vercel preview remained READY.)
- [x] Correct only stale current/final PR-size and branch-protection evidence in the PR description and repository documentation, preserving labelled historical snapshots and review limitations. (The review map now labels 44/142 as historical and 52/154 at `078f618` as the fresh evidence snapshot; it preserves inventory-only/full-review/CI limitation language and records the fresh 404 fact.)
- [x] Push the documentation-only reconciliation and confirm automatic exact-head checks remain green without merging, publishing, configuring secrets, or changing runtime behavior. (Documentation-only head `7506b8a` passed push workflow `33061895092`; matching Vercel preview `dpl_GA2eRhFhNoqA54V4kwnf1RyTetJ4` was `READY`. PR #1 evidence was refreshed without a merge, dashboard publication, production promotion, secret change, or runtime behavior change.)
- [x] Create a concise risk-based PR review map and clarify branch versus public deployment status, architecture/demo workflow, limitations, reproducible commands, and licence/settings owner gates without manufacturing repository signals. (Added `docs/PR_REVIEW_MAP.md`, `docs/PYTHON_REPRODUCIBILITY.md`, and scoped README/deployment-status clarifications.)
- [x] Implement the code-owned Phase 0 release blockers identified on 2026-09-03. (Added conservative obvious-non-MRI rejection before inference, a 4-megapixel upload budget, 512-pixel Grad-CAM bounds, HTTP 503 unavailable readiness, retry-safe deterministic artifact registration, physical-delete-before-metadata behavior, owner-namespace validation, a root MIT licence, and `mysql2` 3.24.3 remediation.)
- [x] Run the Phase 0 deterministic safety matrix before push. (54 Vitest, 103 FastAPI, and 8 ML/data tests pass with critical coverage thresholds; TypeScript/build/bundle pass; production Node and locked Python audits report no known vulnerabilities. The local Playwright browser binary could not be downloaded, so CI browser verification, exact-head preview, and owner-controlled configuration remain post-push gates.)
- [x] Push Phase 0 and verify the exact remote head without merging or promoting production. (Commit `2bbf8ab` passed Actions `33884318987` and Vercel status; the preview reports health/readiness and EXP-005 available with Mode B unavailable. A bad-checksum PNG probe exposed a safe-error gap that is corrected in the next commit.)
- [x] Complete Phase 1A browser privacy and transport reliability hardening. (Analysis state is memory-only with legacy-storage cleanup and blob revocation; inference/report/chat calls have explicit deadlines, strict response schemas, no automatic POST retry, and a bounded PDF response. The local 60/104/8 matrix, coverage thresholds, TypeScript, build, and bundle gate pass.)
- [x] Push Phase 1A and verify exact-head CI plus the new immutable preview, including bad-checksum and obvious-non-MRI HTTP 422 behavior. (Commit `5530711` passed Actions `33885744892`; immutable preview `dpl_Dob2Rw2sDdhuRgKw1QPubwrMcJtc` is READY and returns health/readiness 200 plus safe 422 responses for both probes. A lawful test inference remains owner/data-controlled.)
- [x] Implement Phase 1B shared abuse and receipt-replay state without provisioning credentials. (Optional managed Upstash uses atomic counters and `SET NX EX`, hashed identifiers, bounded deadlines, and a fail-closed required mode. Credential-free development keeps the explicitly non-distributed bounded fallback; focused tests pass.)
- [x] Run the full local Phase 1B verification matrix. (60 Vitest, 110 FastAPI/support, and 8 ML/data tests pass with coverage/type/build/bundle/lock gates; the locked Python runtime audit reports no known vulnerabilities.)
- [x] Push Phase 1B and verify exact-head CI/preview without enabling a billed integration or promoting production. (Commit `f4db074` passed Actions `33887677290`; immutable preview `dpl_9TN2qcTD89CdEvGS98KKadmGALnc` is READY with health/readiness 200 and remains honestly in process-local fallback mode because no managed Redis resource was provisioned.)
- [x] Implement Phase 1C authentication minimisation. (Removed persistent browser user-profile copies and render-time storage writes, purged the legacy key, bounded new sessions to seven days, eliminated per-request `lastSignedIn` database writes for existing sessions, and sanitized auth failure logs; focused tests/typecheck pass.)
- [x] Run the full local Phase 1C matrix. (64 Vitest tests, selected coverage, TypeScript, production build, and the 476,109-byte initial-bundle gate pass.)
- [x] Push Phase 1C and verify exact-head CI/preview. (Commit `d9f6824` passed Actions `33888816644`; matching immutable preview `dpl_5fmbB66NHnhofdu6VNZpBZmVgPQd` is READY.)
- [x] Implement Phase 1D privacy-safe operational logging and response hardening. (Bounded JSON request events omit content/identity/secrets, unhandled errors become correlated safe JSON, and all inference responses receive no-store/nosniff/no-referrer headers; focused regressions pass.)
- [x] Run the full local Phase 1D backend matrix. (113 FastAPI/support and 8 ML/data tests pass with all critical coverage thresholds.)
- [x] Push Phase 1D and verify exact-head headers/log behavior without configuring a paid drain or production alerts. (Commit `3e1bcbe` passed Actions `33889772546`; matching preview `dpl_7vXaAfWFZ7EL68WV46PAt7hnZ5Tf` returned health/readiness 200 with the expected no-store/security headers, while drains and alert ownership remain external gates.)
- [x] Complete the next code-owned Phase 2 ingress-hardening slice without changing Mode A outputs or enabling Mode B. (CORS configuration now fails closed on wildcards, insecure non-loopback HTTP, credentials, paths, queries, fragments, and invalid ports; upload routes reject malformed or over-budget declared sizes before multipart parsing; all early middleware failures preserve a valid bounded request ID.)
- [x] Run Phase 3 exact-head verification and record only observed release evidence. (The local matrix passed 137 FastAPI/support tests, 64 TypeScript tests, 8 ML/data tests, critical coverage thresholds, typecheck, production build, and the 476,109-byte bundle gate. Commit `c084277` passed Actions `33902918337`; matching preview `dpl_CwqB25WZoNic1osDEzetkt2Mg4gm` is READY with health/readiness 200, EXP-005 available, Mode B unavailable, and no production promotion.)
- [x] Correct serverless per-client rate-limit identity without trusting caller-controlled forwarding headers on other runtimes. (Vercel deployments use the platform-overwritten, IP-validated `x-forwarded-for` value only when `VERCEL=1`; local and non-Vercel deployments ignore it, and invalid values fall back to the socket peer.)
- [x] Verify and document the exact proxy-identity hardening head. (Commit `a9f56d8` passed Actions `33922917780`: 143 FastAPI/support tests, 64 TypeScript tests, 8 ML/data tests, coverage, both production audits, browser checks, SBOM, and container smoke. Preview `dpl_5yRRqaceVa8tQx2WiuhEPqyThGhf` is READY with health/readiness 200, EXP-005 available, and Mode B unavailable.)
- [x] Make dashboard production startup fail closed on the exact hosting-assigned port. (Production no longer probes unadvertised alternatives; invalid configuration and initial listen failures produce a generic error-type event and non-zero exit, while bounded fallback remains development-only.)
- [x] Verify and document the exact dashboard-startup hardening head. (Commit `fdf668e` passed Actions `33924136834`: 143 FastAPI/support tests, 74 TypeScript tests, 8 ML/data tests, coverage, both production audits, browser checks, SBOM, and container smoke. The matching READY Vercel deployment is the separate FastAPI preview and is not evidence of a dashboard publication.)
- [x] Fail closed on absent, blank, or weak production dashboard session-signing configuration. (Session operations never use an empty HMAC key; production requires at least 32 UTF-8 bytes before listening, and CI generates an ephemeral test-only key rather than committing one.)
- [x] Verify and document the exact session-signing hardening head. (Commit `4e40f99` passed Actions `33925467097`: 143 FastAPI/support tests, 80 TypeScript tests, 8 ML/data tests, coverage, both production audits, browser checks, SBOM, and container smoke. Matching FastAPI preview `dpl_6PcxACLqLDwaR4aVT9yyxStWs6jM` is READY; real dashboard secret provisioning remains an owner gate.)
- [x] Bind dashboard sessions to the configured application identity. (Production fails before listening without `VITE_APP_ID`; issue and verification require the configured identity, valid same-key tokens from another application are rejected, and the lower-level signer cannot mint a mismatched payload.)
- [x] Verify and document the exact application-binding head. (Commit `16ae35e` passed Actions `33953891043`: 143 FastAPI/support tests, 87 TypeScript tests, 8 ML/data tests, coverage, both audits, browser checks, SBOM, and container smoke. Matching FastAPI preview `dpl_CKW5KPZHivZfmX1uL3EmAMPrrsBq` is READY; real dashboard identity/signing verification remains an owner gate.)
- [x] Harden the managed-storage URL and error boundary. (Provider-issued upload/download targets must be credential-free HTTPS URLs, calls have explicit deadlines, malformed responses fail before use, and upstream response bodies are not exposed through errors.)
- [x] Verify and document the exact managed-storage hardening head. (Commit `96f1a4d` passed Actions `33955911948`: 143 FastAPI/support tests, 96 TypeScript tests, 8 ML/data tests, coverage, both audits, browser checks, SBOM, and container smoke. Matching FastAPI preview `dpl_64gJKEPNDFwW3wvmx2HLYbgYQvew` is READY; live managed-provider save/download/delete remains an owner gate.)
- [x] Minimise dashboard operational failure logs and bound notification delivery. (Database, authentication, OAuth, and notification failures expose only a fixed event plus an allowlisted error type or numeric status; raw exceptions, provider bodies/reason text, configured OAuth endpoints, credentials, and user content are omitted; notification calls have a 10-second deadline.)
- [x] Verify and document the exact operational-log hardening head. (Commit `9dd601d` passed Actions `33957047143`: 143 FastAPI/support tests, 104 TypeScript tests, 8 ML/data tests, coverage, both production audits, browser checks, SBOM, and container smoke. Matching FastAPI preview `dpl_CmSmLDV39ETsh7SBjv9XrbHdpxVb` is READY; a real dashboard log drain, retention policy, alert routing, and provider-backed notification probe remain owner gates.)
- [x] Isolate the dormant voice-transcription network boundary. (Audio downloads require explicit exact-host allowlisting and credential-free HTTPS, refuse redirects and unknown MIME types, enforce declared and streamed 16 MiB limits plus request deadlines, and return no raw network/provider details.)
- [x] Verify and document the exact voice-fetch hardening head. (Commit `9dde29f` passed Actions `33971141046`: 143 FastAPI/support tests, 117 TypeScript tests, 8 ML/data tests, coverage, both production audits, browser checks, SBOM, and container smoke. Matching FastAPI preview `dpl_AG4DUs6ZjN6NjriWgxPKjSEsZXHb` is READY; voice activation, real host configuration, egress controls, and a lawful provider-backed test remain owner gates.)

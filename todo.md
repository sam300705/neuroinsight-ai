# Project TODO

- [x] Preserve the academic/research-only positioning and show an always-visible non-diagnostic disclaimer in the global application shell.
- [x] Create a multi-page responsive navigation experience for landing, analyse, results, scan history, methodology, model performance, limitations, and about pages.
- [x] Build a clearly labelled Mode A 2D PNG/JPEG four-class classification workflow for glioma, meningioma, pituitary tumor, and no tumor.
- [x] Build a clearly labelled Mode B glioma-focused volumetric NIfTI segmentation workflow with supported-scope guidance.
- [x] Implement drag-and-drop upload with extension, MIME type, size, and corrupted-file validation plus compatible error states.
- [x] Implement input-quality checks and manual-review warnings for low-quality, incompatible, or out-of-distribution inputs.
- [x] Display the predicted class and explicitly label model confidence as a non-medical model confidence score.
- [x] Implement calibration and uncertainty status with a validation-derived abstention threshold and manual-review warning state.
- [x] Generate real classifier Grad-CAM artifacts with an opacity slider, colour legend, and persistent coarse-attribution disclaimer.
- [x] Render segmentation masks in a 2D slice overlay with the persistent glioma-focused scope disclaimer.
- [x] Calculate segmentation area only with valid spacing metadata and otherwise return pixel count and image-occupancy percentage.
- [x] Calculate volume only from compatible volumetric metadata and multiple compatible slices.
- [x] Generate academic-use PDF reports containing required identifiers, outputs, limitations, and expert-review recommendation.
- [x] Provide an English/Hindi safe contextual chatbot with minimal structured context, treatment refusal, prompt-injection resistance, and an offline FAQ fallback.
- [x] Persist anonymized scan-history metadata and derived artifacts without retaining raw scans by default.
- [x] Add history search, predicted-class filters, reopen-result support, individual deletion, retention controls, and a confirmed delete-all flow.
- [x] Store PDF reports, Grad-CAM overlays, and segmentation artifacts using S3-compatible storage for history re-download. (Mode A persists only derived PDF and real Grad-CAM artifacts through protected, duplicate-safe registration and signed retrieval; no raw upload is stored. A signed-in browser check saved, retrieved, and deleted a temporary public-demo record. Mode B artifacts correctly remain unavailable until a validated full-volume model exists.)
- [x] Add interactive 3D visualization only for compatible segmented volumes, including rotate, zoom, reset, opacity, highlight, loading/error states, and an algorithmic-geometry disclaimer.
- [x] Provide complete English/Hindi language switching across all pages and chatbot responses. (Active-page copy tests cover both languages; the Hindi browser real-inference check verifies localized experimental-result and protected-save controls while the exact required non-diagnostic notice remains visible.)
- [x] Implement accessible keyboard navigation, labelled controls, focus states, contrast, and colour-independent warning communication. (Cross-route WCAG 2 A/AA axe audit passes; browser zoom is enabled, keyboard skip-link behavior passes on all primary routes, and labelled-control/non-colour warning checks pass.)
- [x] Implement backend schemas, secure upload handling, health/readiness, model information, analysis, reporting, chat, and history endpoints.
- [x] Add reproducible data-download guidance, dataset integrity checks, duplicate detection, manifests, patient-level splitting where available, and data-audit outputs.
- [x] Train and evaluate justified classification and segmentation models only after documented dataset access and provenance verification. (Mode A completed EXP-005 with audited data, held-out image-level evaluation, calibration, and deployment. Mode B was correctly retained unavailable because no suitable full-volume validated segmentation model and evaluation evidence exists; the prior 2D smoke model is not activated.)
- [x] Record actual experiments, calibration, model cards, metrics, limitations, and security/privacy evidence without fabrication.
- [x] Add backend, ML, frontend, accessibility, and end-to-end automated tests; visually verify desktop and mobile workflows. (12 FastAPI, 6 ML/data, and 24 web tests pass; saved Playwright checks cover corrupt upload, real external Mode A display, and skip-link/labelled-control/non-colour-warning accessibility; responsive desktop/mobile captures are recorded.)
- [x] Prepare local and production deployment documentation, Docker/Compose assets where justified, CI, and a verified deployment smoke-test procedure. (Documentation and assets are present; package build, FastAPI/ML/web suites, browser checks, and the separate Vercel ONNX smoke path have been verified.)
- [x] Make the academic and non-diagnostic disclaimer persistently visible in the global shell on desktop and mobile layouts.
- [x] Connect the chatbot to current analysis context and add explicit English/Hindi refusals for prompt injection, hidden prompts, diagnosis, and treatment requests.
- [x] Complete source-provenance verification for the classification dataset, or switch to a fully provenance-cleared source, then rerun classification experiments against the approved dataset. (BDNeuro-MRI v7 is documented from its authoritative Mendeley record with CC BY 4.0 evidence, fixed-split audit, duplicate review, image-level limitations, and EXP-005 rerun.)
- [x] Add a real report timestamp and test required report fields, manual-review text, and optional Grad-CAM/segmentation artifact rendering.
- [x] Implement and verify scan-history artifact retrieval and re-download end to end through protected backend procedures and the history page. (A signed-in public-browser check saved only derived Mode A PDF and real Grad-CAM artifacts, opened the report through an ownership-gated fresh signed URL, and removed the temporary record. No original upload was stored.)
- [x] Wire the analysis upload workflow to a backend validation path and surface corruption or incompatibility errors in the user interface. (Client integration and response-mapping unit tests pass. Browser checks now prove a corrupted image is blocked locally and one lawful public fixed-split image reaches real experimental external inference, displays the mandatory non-clinical result, and shows the protected-save consent control.)
- [x] Add end-to-end coverage for a corrupted image or NIfTI upload in the analysis workflow. (Playwright targets the native MRI input, confirms the image-signature error, and confirms that Validate and continue remains disabled before any inference request.)
- [x] Document calibration evidence or its explicit absence for each analysis mode and explain the resulting deployment gate.
- [x] Add verified security and privacy evidence for raw-scan handling, protected history access, artifact boundaries, deletion confirmation, and scope disclaimers.
- [x] Re-audit authoritative public Mode A sources for licence, provenance, citation, format, case identifiers, split integrity, source-confounding risk, and academic-demo suitability.
- [x] Select and document the most defensible permitted Mode A scope, explicitly retaining image-level rather than patient-independent evaluation when case identifiers are unavailable.
- [x] Train, calibrate where justified, and evaluate real reproducible experimental models using the approved source(s), without fabricating clinical validation.
- [x] Connect real local model inference to predictions, model-confidence labels, Grad-CAM, segmentation, measurements, reports, artifact registration, and protected history retrieval. (Mode A real experimental inference, calibrated score, Grad-CAM, reports, artifact registration, and protected-history retrieval are verified end to end. Mode B intentionally returns unavailable rather than fabricating segmentation, measurements, or 3D geometry without a validated full-volume model.)
- [x] Run local end-to-end checks using non-sensitive demonstration files, including corrupt-upload rejection and real derived-artifact retrieval. (Corrupt rejection, real inference, Grad-CAM, and PDF generation are verified locally and from the published dashboard. A signed-in browser check also saved and retrieved the temporary public-demo report through a fresh signed URL before cleanup.)
- [x] Prepare an HTTPS deployment of the FastAPI validation service, verify health/readiness, and set only exact dashboard and localhost CORS origins after the dashboard URL is known. (Vercel ONNX Runtime deployment passed HTTPS health/readiness, CORS preflight, corrupt-upload rejection, and one real public fixed-split image inference check; dashboard now uses its durable HTTPS origin.)
- [x] Create a publish-ready dashboard checkpoint and provide the user with the required project-interface publication handover; do not represent publishing as clinical validation. (Published at https://neuroaiapp-gtbxy6cw.manus.space as a strictly non-clinical academic demonstration.)
- [x] After dashboard publication, add its final public HTTPS origin to the backend CORS allowlist and re-verify preflight without broadening allowed origins. (Exact-origin preflight succeeds; an unrelated origin receives HTTP 400 without an allow-origin header; browser-level real inference from the public dashboard passes.)
- [x] Add explicit browser accessibility coverage for skip-link keyboard behavior, labelled upload controls, and text plus disabled-state communication for corrupted-file warnings. (Saved Playwright check passed.)
- [x] Add documented contrast and keyboard/focus verification across the main dashboard routes, then update the accessibility evidence record. (Saved axe-core/Playwright audit passes on all eight primary routes.)
- [x] Correct the public home-page model-status message so it accurately identifies the verified live Mode A experimental classifier while preserving Mode B’s unavailable status. (Verified on the refreshed public release.)
- [x] Correct the public performance-page status copy so it reports the documented Mode A experimental image-level evidence and keeps Mode B unavailable. (Verified on the refreshed public route with the EXP-005 image-level metrics and explicit Mode B evidence gate.)
- [x] Audit the supplied college presentation and synopsis against the implemented NeuroInsight AI evidence and identify accurate report-alignment gaps. (See `docs/COLLEGE_REPORT_ALIGNMENT.md`.)
- [x] Add only evidence-consistent college-report improvements, preserving the non-clinical, image-level, and Mode B-unavailable boundaries. (Clarified bilingual public copy, corrected the release-report history statement, and added report-ready project wording.)
- [x] Prepare a separate clinician-support research roadmap covering multi-site data, validated full-volume segmentation, clinician evaluation, workflow testing, privacy governance, and applicable regulatory assessment; do not activate clinical claims or functionality without the required evidence and approvals. (Roadmap explained in plain language; no clinical functionality or claims were activated.)
- [x] Research only officially released, de-identified public brain-MRI sources for future data planning; verify provenance, access terms, de-identification statements, and academic-use permissions without collecting hospital or DNA data, downloading restricted data, retraining models, or changing the deployed system. (See `docs/FUTURE_DATA_SOURCING.md`; the release remains unchanged.)
- [x] Evaluate BRISC 2025 as the primary separately gated next-stage research candidate: verify its official Kaggle terms, provenance, classification/segmentation scope, labels, masks, study-identifier availability, and overlap; obtain user-authorized access without accepting terms on the user’s behalf; audit any permitted data; then train and evaluate only after the audit. Keep the public app unchanged unless explicit evidence gates are met. (The authorised local release passed integrity/pairing checks; EXP-006 was a separate non-promoted classifier experiment. Full-volume Mode B remains unavailable.)
- [x] Prepare and attempt the user-requested technical-supervision workflow for BRISC blockers without relying on unverified external advice. (The connection test returned HTTP 429 before any technical recommendation. A privacy-safe, repository-specific future packet is ready in `docs/BRISC_TECHNICAL_SUPERVISION_PACKET.md`; it must be sent only after the owner restores the service quota, and any future recommendation still requires local verification.)
- [x] Prepare a privacy-safe BRISC technical-supervision packet containing the repository URL/branch, actual audit and EXP-006 results, relevant source files, attempted fixes, and precise questions for a future quota-restored consultation; do not include raw images, signed URLs, tokens, or private user data. (Superseded by the documented packet entry immediately below.)
- [x] Prepare a privacy-safe BRISC technical-supervision packet containing the repository URL/branch, actual audit and EXP-006 results, relevant source files, attempted fixes, and precise questions for a future quota-restored consultation; do not include raw images, signed URLs, tokens, or private user data. (See `docs/BRISC_TECHNICAL_SUPERVISION_PACKET.md`; it has not been sent.)
- [x] Measure the optional frontend payload and lazy-load safe routes or visualization components where it reduces the initial bundle without changing accessibility, bilingual copy, or the current Mode A/Mode B boundaries. (Route-level code splitting reduced the initial JavaScript chunk from 1,407.37 kB to 682.51 kB, a measured 51.50% reduction. TypeScript, 24 Vitest tests, production build, and cross-route WCAG/skip-link browser audit pass; see `docs/FRONTEND_PERFORMANCE.md`.)
- [x] Remove the dormant Three.js renderer from the always-unavailable Mode B results path while retaining an accessible, bilingual explanation that 3D geometry remains unavailable pending validated full-volume segmentation. (The Results chunk fell from 656.31 kB to 41.16 kB, a measured 93.72% reduction; TypeScript, 24 Vitest tests, build, and cross-route WCAG/skip-link audit pass.)
- [x] Reconcile the user-supplied overnight brief with the verified project state, correcting only genuinely stale capability statements and preserving the completed protected-artifact evidence. (Corrected `PROJECT_STATUS.md`, `docs/OPEN_GATES.md`, `docs/EXTERNAL_PREREQUISITES.md`, `docs/SECURITY_PRIVACY_EVIDENCE.md`, and stale public test count.)
- [x] Finish the BRISC integrity, mask-pairing, duplicate, and leakage-resistant split audit; document exact results and keep raw data outside the repository. (See `docs/BRISC_AUDIT.md`; the local raw data and generated manifests remain outside Git.)
- [x] Add a canonical capability manifest and consistency checks only after auditing the current implementation, deployment evidence, and user-facing copy. (Added `docs/CAPABILITY_MANIFEST.md` and a passing regression test.)
- [x] Prepare a morning checklist that records only unavoidable manual gates, including any unavailable technical-supervision quota and model-promotion requirements. (Added `docs/MORNING_SETUP_CHECKLIST.md`.)
- [x] Assess local training resources and run only evidence-gated BRISC experiments that do not modify the live service; retain Mode B as unavailable unless its separate release gate is met. (No GPU was available. A separate CPU-only EXP-006 ResNet18 head-only run used the sanitised BRISC manifests and was not promoted because its official image-level test result was lower than EXP-005.)
- [x] Verify the remote GitHub baseline and create a dedicated feature branch for the requested safe NeuroInsight AI improvements. (Remote `github/main` is at `26498b5`; local verified checkpoints were ahead by seven commits. Created `feature/overnight-safe-improvements` from the local improvement state.)
- [x] Reconcile or implement safe unblocked improvements for protected History artifacts, lifecycle documentation, capability/status records, README, checklist, CI, lazy loading, authorization coverage, and the unactivated case-disjoint Mode B research pipeline. (Added ownership-lifecycle helpers/tests, case-disjoint full-volume manifest preparation/tests, CI safeguards, and synchronized status/evidence records; the existing README, checklist, manifest, protected-history workflow, and bundle reductions are included on the feature branch.)
- [ ] Run the exact repository verification suite, create logical commits, push the feature branch, and open a GitHub pull request with tested evidence and remaining manual gates.

# Manual Gates

This record lists only actions that cannot be safely automated in the current project. It is not a release approval.

| Gate | Why it is owner-controlled | Exact next action | Risk if skipped |
|---|---|---|---|
| Authorize any next managed-dashboard publication | The Express 5 deployment recovery was already owner-approved and published at checkpoint `409f8a70`. The managed project auto-publishes on checkpoint, while the current PR hardening is a material public change and must not be republished without a fresh owner decision. | Review the current PR #1 head and explicitly authorize a new managed-dashboard publication only if the verified branch changes should become public. | The already-live academic dashboard remains on the prior owner-approved recovery; newer hardening remains PR/preview-only. |
| Protect `main` | A final read-only GitHub API check on 2026-08-27 returned `404 Branch not protected`. The available GitHub integration previously rejected personal-repository branch-protection payloads containing the documented review restrictions. | In GitHub **Settings → Branches**, protect `main`: require pull requests, require the `verify` check, require conversation resolution, and disallow force pushes/deletions. | A direct push can bypass review and the CI evidence gate. |
| Verify physical artifact deletion | Phase 0 code now calls `v1/storage/delete` for owner-scoped derived keys before metadata removal and retains metadata on failure. Unit coverage cannot prove the managed provider behavior. | In a non-production account, save and delete one synthetic derived artifact, then verify it can no longer be fetched. | Physical-erasure claims cannot be made until provider integration passes. |
| Configure report signing | The PR preview has no configured report receipt secret. | Add a strong server-only `ANALYSIS_RECEIPT_SECRET` to the **inference service** preview/production environment, never the browser, then verify classify → receipt → PDF and replay behavior before release. | PDF/report saving remains intentionally unavailable; report-integrity guarantees must not be advertised. |
| Provision shared abuse/replay state | The code supports managed Upstash Redis, but this task has no authority to create a billed integration or expose credentials. Current preview behavior therefore uses the documented process-local fallback. | Provision Upstash through the Vercel Marketplace, set server-only `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN`, enable `REQUIRE_DISTRIBUTED_CONTROLS=true`, then verify readiness, cross-instance limits, single-use receipts, store outage 503 behavior, retention, and spend alerts. | Multi-instance rate-limit and receipt-replay guarantees cannot be claimed; public abuse can bypass per-instance state. |
| Verify Mode A artifact initialization before any new Vercel production deployment | Phase 0 preview `2bbf8ab` reports Mode A available, but a bad-checksum PNG probe exposed an HTTP 500 decoder path; the local Phase 1A correction is not yet preview-verified. | On the next exact-head preview verify `/health`, `/ready`, `/api/v1/model-info`, bad-checksum and obvious-non-MRI HTTP 422 behavior, and one lawful test inference before any merge or production promotion. | A new production deployment could differ from the locally tested error handling or model path. |
| Full-volume Mode B research | A lawful full-volume cohort, compute, and release evidence are not available in this task. | Review source terms, provide approved compute, then conduct case-disjoint training and locked held-out evaluation under a separate research protocol. | Mode B must remain unavailable. |
| Legal and privacy copy review | Product policy text is engineering draft material, not legal advice. | Have the owner or qualified counsel review privacy, acceptable-use, retention, and collaboration language before public reliance. | Legal or regulatory claims could be misunderstood. |
| Optional technical supervision | The previously configured external service returned HTTP 429. | Restore owner-controlled quota and send the prepared privacy-safe review packet; verify any advice locally. | No external technical review has been received. |

> **Invariant:** “This system is not a medical diagnosis and must not replace a qualified radiologist.”

# Compatibility entry point. Do not edit package pins here.
# Generate this exact runtime set from backend/pyproject.toml via:
# uv lock --directory backend && uv export --directory backend --locked --no-dev --no-emit-project --no-hashes --output-file requirements.lock
-r requirements.lock
